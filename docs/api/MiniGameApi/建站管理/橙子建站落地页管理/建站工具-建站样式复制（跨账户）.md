通过此接口，用户可以实现站点的转赠功能，将某一客户的站点复制给其他特定的客户，转赠成功后，被转增的投放账户下会新增一个站点id（内容同原站点），但会清空原站点下的资产信息。 不限制主体，同客户不能进行转赠操作

用户在传入请求参数`site_ids`站点id列表时，请确保id的正确性，存在错误将导致转赠全部失败！

关键信息

详情

是否支持SDK

支持

权限点及应用类型

了解如何 [申请授权&生成授权](https://open.oceanengine.com/labels/7/docs/1803016515293203?origin=metadata)

权限点:  工具-建站管理-创建、修改、复制、转赠站点

应用类型:  自研投放应用-客户/代理商/独立三方站点/群峰上架服务、提供第三方服务-客户/代理商/独立三方站点/群峰上架服务

接入能力范围:  巨量 PC

支持的账户类型

客户投放账户

##### 请求地址

<https://ad.oceanengine.com/open_api/2/tools/site/handsel/>
[可视化调试](https://open.oceanengine.com/tools/visual_debug.html?docId=1696710623352844)
[SDK下载](https://open.oceanengine.com/labels/7/docs/1773083358100557)
[MCP调用](https://open.oceanengine.com/labels/7/docs/1847394521222599)

##### 请求方法

POST

##### 请求Header

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| Content-Type必填 | string | 请求消息类型，允许值：`application/json` |
| Access-Token必填 | string | 授权access-token，获取方法见接口文档[【获取Access-Token】](https://open.oceanengine.com/labels/7/docs/1696710505596940) |

##### 请求参数

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| advertiser\_id必填 | number | 客户id，范围：1 <= advertiser\_id <= `MAX_INT64` |
| site\_ids必填 | string[] | 站点id列表, min\_size=1, max\_size=20 |
| target\_advertiser\_ids必填 | string[] | 目标客户id列表, min\_size=1, max\_size=20 |

##### 请求示例

**Python**

```python
def updateStatusSite():
import requests
open_api_url_prefix = "https://ad.oceanengine.com/open_api/"
uri = "2/tools/site/handsel/"
url = open_api_url_prefix + uri
params={
"advertiser_id": 0,
"site_ids": ["685","6996"],
"target_advertiser_ids": ["2","3"],
}
headers = {"Access-Token": "xxx"}
rsp = requests.post(url, json=params, headers=headers)
rsp_data = rsp.json()
return rsp_data
​
```

---

**Java**

```java
public JSONObject updateStatusSite() {
String access_token = "xxx";
final Long advertiser_id = 0L; //操作的广告主id
final String[] site_ids = new string[] {"110","111"};
final String[] target_advertiser_ids = new string[] {"110","111"};
// 请求地址
String open_api_url_prefix = "https://ad.oceanengine.com/open_api/2/";
String uri = "tools/site/handsel/";
​
// 请求参数
Map data = new HashMap(){
{
put("advertiser_id",advertiser_id);
put("site_ids",site_ids);
put("target_advertiser_ids",target_advertiser_ids);
}
};
​
// 构造请求
HttpPost httpEntity = new HttpPost(open_api_url_prefix + uri);
​
httpEntity.setHeader("Access-Token",access_token);
​
CloseableHttpResponse response = null;
CloseableHttpClient client = null;
​
try {
client = HttpClientBuilder.create().build();
httpEntity.setEntity(new StringEntity(JSONObject.toJSONString(data), ContentType.APPLICATION_JSON));
​
response = client.execute(httpEntity);
if (response != null && response.getStatusLine().getStatusCode() == 200) {
BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(response.getEntity().getContent()));
StringBuffer result = new StringBuffer();
String line = "";
while((line = bufferedReader.readLine()) != null) {
result.append(line);
}
bufferedReader.close();
return JSONObject.parseObject(result.toString());
}
​
} catch (ClientProtocolException e) {
e.printStackTrace();
} catch (IOException e) {
e.printStackTrace();
} finally {
try {
if (response != null) {
response.close();
}
client.close();
} catch (IOException e) {
e.printStackTrace();
}
}
return null;
}
​
```

---

**Php**

```php
$client = new http\Client;
$request = new http\Client\Request;
​
$body = new http\Message\Body;
$body->append('{
"advertiser_id": {{advertiser_id}},
"site_ids":["110","111"],
"target_advertiser_ids": ["2","3"],
);
​
$request->setRequestUrl('https://ad.oceanengine.com/open_api/2/tools/site/handsel/');
$request->setRequestMethod('POST');
$request->setBody($body);
​
$request->setHeaders(array(
'Access-Token' => '{{access_token}}',
'Content-Type' => 'application/json'
));
​
$client->enqueue($request)->send();
$response = $client->getResponse();
​
echo $response->getBody();
​
```

---

**Curl**

```bash
POST /open_api/2/tools/site/handsel/ HTTP/1.1
Host: ad.oceanengine.com
Content-Type: application/json
Access-Token: xxx
{
"advertiser_id": 0,
"site_ids":["110","111"],
"target_advertiser_ids": ["2","3"]
}
​
```

##### 应答字段

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| code | number | 返回码,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| message | string | 返回信息,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| data | json | json返回值 |
| success\_list | object | 转赠成功列表，整体失败不返回该列表 |
| origin\_site\_id | string | 返回转赠成功的原`site_ids` |
| target\_advertiser\_id | string | 返回转赠成功的目标客户id |
| site\_id | string | 转赠成功后的生成的新站点id，失败的数据无此参数返回 |
| error\_list | object | 转赠失败列表，整体成功不返回该列表 |
| origin\_site\_id | string | 返回转赠失败的原`site_ids` |
| target\_advertiser\_id | string | 返回转赠失败的目标客户id |
| error\_reason | string | 失败原因，当对`origin_site_id`的操作失败时，返回`error_reason`字段，成功无 |
| request\_id | string | 请求日志id |

##### 应答示例

```
{
"message": "OK",
"code": 0,
"data": {
"success_list": [
    {
    "origin_site_id":"6232",
    "target_advertiser_id":"123123",
    "site_id": "6892"
    }
],
"error_list": [
    {
    "origin_site_id":"6232",
    "target_advertiser_id":"123123",
    "error_reason": "xxxxx"
    }
],
"request_id": "11000000"
}
​
```

