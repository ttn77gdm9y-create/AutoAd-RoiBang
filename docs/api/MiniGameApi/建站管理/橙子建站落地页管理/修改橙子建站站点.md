通过此接口，用户可以修改站点的基本信息。

> 基于应用下载安全要求，在创建和修改站点接口中，如果希望添加应用下载链接，有以下场景适用：  
> ①DownloadEvent类型的按钮组件可以填入安卓/IOS下载链接  
> 除以上情况外其他建站组件中的URL参数都不允许填入应用下载链接，如果填入将会创建失败。

* 目前`bricks`不支持部分更新，仅支持全量更新，更新`bricks`时，需要传递完整`bricks`信息，除需要更新的`bricks`信息以外，其余`bricks`中的信息需要和创建时保持不变。修改成功时，无业务参数返回！
* 图片大小需要在2M之内，否则会报错！
* 快应用链接仅支持以下三类  
  http://hapjs.org/app//[path][?key=value]  
  https://hapjs.org/app//[path][?key=value]  
  hap://app//[paht][?key=value]

关键信息

详情

接口更新动态

请关注当前接口近期改动和即将变动情况

[更新日志：2026年04月14日](https://open.oceanengine.com/changelog/1862437548699648?origin=metadata)

是否支持SDK

支持

权限点及应用类型

了解如何 [申请授权&生成授权](https://open.oceanengine.com/labels/7/docs/1803016515293203?origin=metadata)

权限点:  工具-建站管理-创建、修改、复制、转赠站点

应用类型:  自研投放应用-客户/代理商/独立三方站点/群峰上架服务、提供第三方服务-客户/代理商/独立三方站点/群峰上架服务

接入能力范围:  巨量 PC

支持的账户类型

客户投放账户

### 请求地址

<https://ad.oceanengine.com/open_api/2/tools/site/update/>
[可视化调试](https://open.oceanengine.com/tools/visual_debug.html?docId=1696710618300431)
[SDK下载](https://open.oceanengine.com/labels/7/docs/1773083358100557)
[MCP调用](https://open.oceanengine.com/labels/7/docs/1847394521222599)

### 请求方法

POST

### 请求Header

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| Content-Type必填 | string | 请求消息类型，允许值：`application/json` |
| Access-Token必填 | string | 授权access-token，获取方法见接口文档[【获取Access-Token】](https://open.oceanengine.com/labels/7/docs/1696710505596940) |

### 请求参数

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| advertiser\_id必填 | number | 客户id，范围：1 <= advertiser\_id <= `MAX_INT64` |
| site\_id必填 | string | 站点id，范围：长度 >= 1 |
| name | string | 站点名称，范围：长度 >= 1 |
| bricks必填 | object | 站点业务数据。具体的格式见下方的4大组件格式，list中每个object在站点页面上按照顺序从上到下排列。目前只支持全量更新bricks. [ ，  ] 视频组件 图片组件 |

## 微信小程序/微信小游戏组件 新增

| **字段** | **类型** | **描述** |
| --- | --- | --- |
| name必填 | string | 组件类型，  微信小程序组件类型"XrWechatApplet"  微信小游戏组件类型"XrWechatGame" |
| instance\_id必填 | number | 小游戏ID |
| game\_path | string | 小游戏路径参数 |
| setting | object | 设置 |
| style\_type | number | 允许值：  `DESC_BUTTON` 样式1（下图以微信小游戏组件为例）    `WITHOUT_DESC_BUTTON`样式2    `BUTTON`样式3 |
| background | object | style\_type = `DESC_BUTTON` 样式1 时生效，样式-背景设置 |
| type | string | 背景设置  允许值：`color`使用颜色；`image`使用图片 |
| color | string | 当type = `color`时传入生效 |
| image | object | 当type = `image`时传入生效 |
| ic\_id | string | 图片ID(素材图片id)，对应[【获取图片素材】](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710601254912)接口获得的image\_url和图片ic\_id 必须传一个。如果都传，取image\_url值。 |
| image\_url | string | 图片链接，用户自行上传图片url，image\_url 和图片ic\_id 必须传一个。  如果都传，取image\_url值 |
| image | object | style\_type = `WITHOUT_DESC_BUTTON`样式2 时生效，内容设置-背景图 |
| ic\_id | string | 图片ID(素材图片id)，对应[【获取图片素材】](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710601254912)接口获得的image\_url和图片ic\_id 必须传一个。如果都传，取image\_url值。 |
| image\_url | string | 图片链接，用户自行上传图片url，image\_url 和图片ic\_id 必须传一个。  如果都传，取image\_url值 |
| introduce\_type | string | 内容简介，  允许值：`text`文本，`label` 标签 |
| items | string[] | 当introduce\_type = `label`时，  当label = 标签文案，最多支持传入2个 |
| title | object | 小游戏/小程序名称 |
| color | string | 小游戏/小程序名称颜色 |
| font\_size | number | 小游戏/小程序名称字号 |
| label | object | 小游戏/小程序简介，当introduce\_type = `text`时生效 |
| color | string | 文本颜色信息 |
| font\_size | number | 字号,默认值为 |
| text | string | 介绍文案 |
| avatar | object | Logo 图片，宽高比建议为1:1，图片无水印 |
| ic\_id | string | 图片ID(素材图片id)，对应[【获取图片素材】](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710601254912)接口获得的image\_url和图片ic\_id 必须传一个。如果都传，取image\_url值。 |
| image\_url | string | 图片链接，用户自行上传图片url，image\_url 和图片ic\_id 必须传一个。  如果都传，取image\_url值 |
| button | object | 按钮 |
| color | string | 按钮文案颜色 |
| font\_size | number | 按钮文案字号 |
| background\_color | string | 按钮填充颜色 |
| border\_color | string | 按钮边框颜色 |
| border\_width | number | 按钮边框粗细 |
| border\_radius | number | 按钮圆角大小 |
| x | number | 横轴，代表当前组件在布局中的左右位置，默认为`0` |
| y | number | 纵轴，代表当前组件在布局中的上下位置，默认为`0` |
| width | number | 宽度，默认为375，范围：width >= 1 |
| height | number | 高度，默认为225，范围：height >= 1 |

### 视频组件

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| name必填 | string | 组件类型，填写“XrVideo” |
| local\_source | json | 本地视频，与在线视频传其中一个 |
| vid | string | 视频云id，`local_source`不空时必填 |
| poster条件必填 | string | 视频封面图信息,`local_source`不空时，必填 支持填写在【[获取图片素材](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710601254912)】返回的图片`id`（推荐） 和自行上传的图片url |
| online\_source | json | 在线视频信息，与本地视频信息传其中一个 |
| origin\_url条件必填 | string | 视频原始地址，例如https://v.youku.com/v\_show/id\_xxx.html ，`online_source`不空时必填 |
| poster\_url | string | 封面图url，用户自定义上传 |
| x | number | 横轴，默认为`0` |
| y | number | 纵轴，默认为`0` |
| width | number | 宽度，默认为375，范围：width >= 1 |
| height | number | 高度，默认为225，范围：height >= 1 |
| float | string | 站点组件浮动方式: `none`（默认为无) 其他值可选: `top`（置顶） `right`（居右） `bottom`（置底） |

### 图片组件

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| name必填 | string | 组件类型，填写“XrPicture” |
| image\_url | string | 图片链接，用户自行上传图片url，`image_url` 和图片`ic_id` 必须传一个。 如果都传，取`image_url`值 |
| ic\_id | string | 图片ID(素材图片id)，对应【[获取图片素材](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710601254912)】接口获得的`id``image_url`和图片`ic_id` 必须传一个。 如果都传，取`image_url`值。 |
| link | json | 图片跳转链接信息 |
| url条件必填 | string | 链接地址，当link\_type为`url`时，必填 |
| scheme条件必填 | string | scheme地址，当link\_type为`scheme`时，必填 |
| quick\_app条件必填 | string | 快应用地址，当link\_type为`quick_app`时，必填 获取快应用链接可参考【[快应用官方网站](https://www.quickapp.cn/?aadvid=3526908054)】 |
| link\_type条件必填 | string | 链接类型，可传值有: `"url"`：链接地址, `"sheme"`：scheme地址, `"quickApp"`：快应用地址, 当传递链接信息参数时，必填 |
| border\_radius | number | 圆角，默认为`0`，范围：border\_radius >= 0 |
| is\_cover | number | 是否保持宽高比 ， `1`: 表示保持宽高比， `0`：不保持。默认为`0` |
| x | number | 横轴，默认为`0` |
| y | number | 纵轴，默认为`0` |
| width | number | 宽度，默认为375，范围：width >= 1 |
| height | number | 高度，默认为225，范围：height >= 1 |
| float | string | 站点组件浮动方式: `none`（默认为无) 其他值可选: `top`（置顶） `right`（居右） `bottom`（置底） |

### 组图组件

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| name必填 | string | 组件类型，填写“XrPictureGroup” |
| group\_type | string | 组图类型，默认`normal`允许值： 常规居中`normal`平铺拉伸`carousel`拼接覆盖`coverflow`【效果见[橙子建站](https://www.chengzijianzhan.com/)界面】 |
| auto\_play | number | 是否自动播放，默认自动播放 允许值：`0`（不自动），`1`（自动播放） |
| group\_content必填 | object[] | 组图内容列表 |
| image\_url | string | 图片链接，用户自行上传图片url，`image_url` 和图片`ic_id` 必须传一个。 如果都传，取`image_url`值 |
| ic\_id | string | 图片ID(素材图片id)，【[获取图片素材](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710601254912)】接口获得的`id``image_url`和图片`ic_id` 必须传一个。 如果都传，取`image_url`值。 |
| tag | string | 标签，默认为`""` |
| link | json | 图片跳转链接信息 |
| url条件必填 | string | 链接地址，当link\_type为`url`时，必填 |
| scheme条件必填 | string | scheme地址，当link\_type为`scheme`时，必填 |
| quick\_app条件必填 | string | 快应用地址，当link\_type为`quick_app`时，必填 获取快应用链接可参考【[快应用官方网站](https://www.quickapp.cn/?aadvid=3526908054)】 |
| link\_type条件必填 | string | 链接类型，可传值有: `"url"`：链接地址, `"sheme"`：scheme地址, `"quickApp"`：快应用地址, 当传递链接信息参数时，必填 |
| x | number | 横轴，默认为`0` |
| y | number | 纵轴，默认为`0` |
| width | number | 宽度，默认为375，范围：width >= 1 |
| height | number | 高度，默认为225，范围：height >= 1 |
| float | string | 站点组件浮动方式: `none`（默认为无) 其他值可选: `top`（置顶） `right`（居右） `bottom`（置底） |

### 文本组件

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| name必填 | string | 组件类型，填写“XrSimpleText” |
| content必填 | string | 文本内容 长度至少为1 |
| color | string | 文本颜色信息，默认`rgba（51,0,0,1）` |
| link | json | 文本跳转链接信息 |
| url条件必填 | string | 链接地址，当link\_type为`url`时，必填 |
| scheme条件必填 | string | scheme地址，当link\_type为`scheme`时，必填 |
| quick\_app条件必填 | string | 快应用地址，当link\_type为`quick_app`时，必填 获取快应用链接可参考【[快应用官方网站](https://www.quickapp.cn/?aadvid=3526908054)】 |
| link\_type条件必填 | string | 链接类型，可传值有: `"url"`：链接地址, `"sheme"`：scheme地址, `"quickApp"`：快应用地址, 当传递链接信息参数时，必填 |
| font\_family | number | 文本字体，例如"font-1" 文本字体支持如下： `font-x`x 的值可为：`0`（系统默认字体）x`4`（方正仿宋简体） `7`（方正黑体简体） `10`（方正楷体简体） `13`（方正书宋简体） `14`（华文琥珀） `15`（华文隶书） `16`（华文新魏） `17`（华文行楷） `23`（濑户字体） `24`（庞门正道标题体） `32`（书体坊颜体） `90`（文泉驿正黑） `94`（杨任东竹石体） `95`（站酷高端黑） `96`（站酷酷黑） `97`（站酷快乐体） `100`（装甲明朝体） `101`（思源黑体粗体） `102`（思源黑体黑体） `103`（思源黑体细体） `104`（思源黑体纤细） `105`（思源黑体中黑） `106`（思源黑体常规） `107`（思源黑体极细） |
| font\_size | number | 字号 默认值为`14` |
| font\_style | string[] | 文字样式，可选值： 黑体`"bold"`斜体`"italic"`下划线`"underline"`, 默认为无样式。可随意组合 |
| letter\_spacing | number | 字间距，默认值为`0` |
| line\_height | number | 行高，例如：1.3 |
| text\_align | string | 对齐方式，可选值： `"left"`(左对齐) `"right"`(左对齐) `"center"`(居中) 默认为左对齐 |
| x | number | 横轴，代表当前组件在布局中的左右位置，默认为`0` |
| y | number | 纵轴，代表当前组件在布局中的上下位置，默认为`0` |
| width | number | 宽度，默认为375，范围：width >= 1 |
| height | number | 高度，默认为225，范围：height >= 1 |
| float | string | 站点组件浮动方式: `none`（默认为无) 其他值可选: `top`（置顶） `right`（居右） `bottom`（置底） |

### 富文本组件

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| name必填 | string | 组件类型，填写“XrRichText” |
| content必填 | string | 富文本内容 例如：”请添加文本内容“ |
| link | json | 文本跳转链接信息 |
| url条件必填 | string | 链接地址，当link\_type为`url`时，必填 |
| scheme条件必填 | string | scheme地址，当link\_type为`scheme`时，必填 |
| quick\_app条件必填 | string | 快应用地址，当link\_type为`quick_app`时，必填 获取快应用链接可参考【[快应用官方网站](https://www.quickapp.cn/?aadvid=3526908054)】 |
| link\_type条件必填 | string | 链接类型，可传值有: `"url"`：链接地址, `"sheme"`：scheme地址, `"quickApp"`：快应用地址, 当传递链接信息参数时，必填 |
| x | number | 横轴，代表当前组件在布局中的左右位置，默认为`0` |
| y | number | 纵轴，代表当前组件在布局中的上下位置，默认为`0` |
| width | number | 宽度，默认为375，范围：width >= 1 |
| height | number | 高度，默认为225，范围：height >= 1 |
| float | string | 站点组件浮动方式: `none`（默认为无) 其他值可选: `top`（置顶） `right`（居右） `bottom`（置底） |

### 表单组件

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| name必填 | string | 组件类型，填写“XrForm” |
| form\_data | object | 表单数据 |
| instance\_id条件必填 | number | 已有表单，如果传入`instance_id`，以此为准，其他表单数据不再读取 当传表单数据时，`submit_text`和`name`为空时必填 |
| submit\_text条件必填 | string | 提交文案 当传表单数据时，无`instance_id`时， 必填 |
| name条件必填 | string | 表单名称 当传表单数据时，无`instance_id`时， 必填 |
| elements | object[] | 表单元素 |
| label | string | 元素标签 |
| allow\_empty | number | 是否允许为空 允许值：`0`（必填），`1`（可为空） |
| type条件必填 | string | 表单元素类型 允许值： `NAME`（姓名） `TELEPHONE`（电话） `EMAIL`（邮箱） `NUMBER`（数值） `SEX`（性别） `DATE`（日期） `CITY`（城市） `TEXT`（文本） `TEXTAREA`（文本域) 当`allow_empty`为`0`时必填 |
| is\_unique | number | 是否可重复 允许值：`0`（唯一），`1`（可重复） |
| success\_link | json | 提交成功跳转链接 |
| url条件必填 | string | 链接地址，当link\_type为`url`时，必填 |
| scheme条件必填 | string | scheme地址，当link\_type为`scheme`时，必填 |
| quick\_app条件必填 | string | 快应用地址，当link\_type为`quick_app`时，必填 |
| link\_type条件必填 | string | 链接类型，可传值有: `"url"`：链接地址, `"sheme"`：scheme地址, `"quickApp"`：快应用地址, 当传递链接信息参数时，必填 |
| failure\_link | json | 提交失败跳转链接 |
| url条件必填 | string | 链接地址，当link\_type为`url`时，必填 |
| scheme条件必填 | string | scheme地址，当link\_type为`scheme`时，必填 |
| quick\_app条件必填 | string | 快应用地址，当link\_type为`quick_app`时，必填 获取快应用链接可参考【[快应用官方网站](https://www.quickapp.cn/?aadvid=3526908054)】 |
| link\_type条件必填 | string | 链接类型，可传值有: `"url"`：链接地址、 `"sheme"`：scheme地址、 `"quickApp"`：快应用, 当传递链接信息参数时，必填 |
| linkable | number | 表单提交后是否跳转 允许值：`0`（不跳转），`1`（跳转） |
| success\_tip | string | 用户提交成功提示语表示用户填写表单成功后以小窗方式弹出的提示信息，比如告诉用户24小时后商家会主动联系用户，长度不超过128，默认值“表单提交成功” |
| x | number | 横轴，代表当前组件在布局中的左右位置，默认为`0` |
| y | number | 纵轴，代表当前组件在布局中的上下位置，默认为`0` |
| width | number | 宽度，默认为375，范围：width >= 1 |
| height | number | 高度，默认为225，范围：height >= 1 |
| float | string | 站点组件浮动方式: `none`（默认为无), 其他值可选: `top`（置顶） `right`（居右） `bottom`（置底） |

### 按钮组件

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| name必填 | string | 组件类型，填写“XrButton” |
| icon | string | 图标信息，icon类型值： `none`(缺省) `consult`（咨询图标） `telephone`(电话图标) `download`(下载图标) |
| bg\_type | string | 背景类型，可选值：`"color"`(默认值)，`"image"`(图片) |
| text条件必填 | string | 文案，`bg_type` 为`"color"`时，有效且必填 |
| color | string | 文案颜色信息，默认值`rgba(255,255,255,1)` |
| font\_size | number | 文案字号,默认值`16` |
| bg\_color | string | 背景颜色信息，比如“rgba(255,255,255,1)”,默认为无 |
| bg\_image条件必填 | string | 背景图片，bg\_type 为`"image"`时，有效且必填 可自行上传图片url，或从[获取图片素材](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710601254912)获取图片ID：`id` |
| border\_color | string | 边框颜色信息，比如“rgba(255,255,255,1)”,默认为无 |
| border\_width | number | 边框粗细，默认`0` |
| border\_radius | number | 边框圆角，默认为`0`，范围：radius >= 0 |
| events | object[] | 事件列表信息 |
| trigger | object | 触发 |
| type | string | 触发事件，可选值： `"click"`（点击） `"longTouch"`（长按） |
| touch\_seconds条件必填 | numer | 长按时间（秒）：触发时间为`"longTouch"`类型，必填 长按时间 >= 1 |
| behavior必填 | object | 事件行为描述，behavior目前已开放DownloadEvent 和LinkEvent以及TelephoneEvent. 具体传递方式见下表。必填 |
| package\_info | object | 应用下载样式 |
| type | string | 展示样式:"imageText"：图片文字；"image"：图片 |
| image | string | type为“image”样式时有效，可自行上传图片url，或从[获取图片素材](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710601254912)获取图片ID：`id` |
| background | object | 背景信息，type为“imageText”样式时有效 |
| type | string | 背景类型："color"：颜色；"image"：图片 |
| color | string | 背景颜色信息，比如“rgba(255,255,255,1)”,默认为无 |
| image | string | 背景图片，background的type 为"image"时，可自行上传图片url，或从[获取图片素材](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710601254912)获取图片ID：`id` |
| description | object | 应用包描述，type为“imageText”样式时有效 |
| type | string | 描述展示类型:"label"：标签；"text"：文本 |
| labels | string[] | 标签内容，description的type为“label”时有效 |
| color | string | 文字颜色，比如“rgba(255,255,255,1)”,默认为无 |
| font\_size | number | 文字大小，默认`15` |
| title | object | 标题配置，type为“imageText”样式时有效 |
| color | string | 文字颜色，比如“rgba(255,255,255,1)”,默认为无 |
| font\_size | number | 文字大小，默认`15` |
| x | number | 横轴，代表当前组件在布局中的左右位置，默认为`0` |
| y | number | 纵轴，代表当前组件在布局中的上下位置，默认为`0` |
| width | number | 宽度，默认为375，范围：width >= 1 |
| height | number | 高度，默认为225，范围：height >= 1 |
| float | string | 站点组件浮动方式: `none`（默认无) 其他值可选: `top`（置顶） `right`（居右） `bottom`（置底） |

#### DownloadEvent事件行为描述

特殊说明：基于应用下载安全要求，在创建和修改站点接口中，如果希望添加应用下载链接，有两种情况：①DownloadEvent类型的按钮组件可以填入安卓/IOS下载链接；②LinkEvent类型的按钮组件的URL支持落地页链接，不支持下载链接，仅DownloadEvent事件下可填写下载链接；综上，除了这两种情况外其他建站组件中的URL参数都不允许填入应用下载链接，如果填入将会创建失败。

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| name必填 | string | 事件名称，传“DownloadEvent” |
| ios\_link | json | ios链接信息 |
| link\_type条件必填 | string | 链接类型，可传值有`"url"`当传递链接信息参数时必填 |
| url | string | 下载地址 当link\_type为`url`时必填 |
| description | string | 应用描述，为了展示效果，推荐12个中文字符长度 |
| android\_link | json | 安卓链接信息 |
| link\_type条件必填 | string | 链接类型，可传值有: `"url"`：链接地址， `"quickApp"`：快应用地址， 当传递链接信息参数时，必填 |
| url条件必填 | string | 链接地址，当link\_type为`url`时，必填 |
| quick\_app条件必填 | string | 快应用地址，当link\_type为`quick_app`时，必填 获取快应用链接可参考【[快应用官方网站](https://www.quickapp.cn/?aadvid=3526908054)】 |
| asset\_id条件必填 | string | 快应用资产id，当link\_type为`quick_app`时，必填  获取快应用资产ID可参考【[获取账户下资产列表（新）](https://open.oceanengine.com/labels/7/docs/1800985709803914)】接口 |
| description | string | 应用描述，为了展示效果，推荐12个中文字符长度 |

#### LinkEvent事件行为描述

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| name必填 | string | 事件名称，传“LinkEvent” |
| link | object | 链接信息（支持落地页链接，不支持下载链接，仅DownloadEvent事件下可填写下载链接） |
| url条件必填 | string | 链接地址，当link\_type为`url`时，必填 |
| scheme条件必填 | string | scheme地址，当link\_type为`scheme`时，必填 |
| quick\_app条件必填 | string | 快应用地址，当link\_type为`quick_app`时，必填 获取快应用链接可参考【[快应用官方网站](https://www.quickapp.cn/?aadvid=3526908054)】 |
| link\_type条件必填 | string | 链接类型，可传值有: `"url"`：链接地址, `"sheme"`：scheme地址, `"quickApp"`：快应用地址, 当传递链接信息参数时，必填 |

#### TelephoneEvent事件行为描述

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| name必填 | string | 事件名称，传“TelephoneEvent” 1 <= `name`长度值 <= 20 |
| smart\_phone | object | 智能电话信息 |
| instance\_id | number | 表单ID，如果传入`instance_id`，以此为准，其他智能电话数据不再读取 |
| name条件必填 | string | 新建智能电话的名称，无`instance_id`时， 必填 |
| phone\_number条件必填 | number | 电话号码，无`instance_id`时， 必填 |
| call\_display | number | 是否展示真实号码，默认现实中间虚拟号码。 默认值：`0`（中间虚拟号码） 允许值：`0`（中间虚拟号码）、`1`（用户真实号码） 【注】显示用户真实号码可能会被运营商拦截，发生拨打异常情况时可选择来电展示中间虚拟号码。 |

### 请求示例

**Python**

```python
def updateSite():
    import requests
    open_api_url_prefix = "https://ad.oceanengine.com/open_api/"
    uri = "2/tools/site/update/"
    url = open_api_url_prefix + uri
    params={
            "advertiser_id": 0,
             "name":"site_name",
             "bricks":[
                {
                    "name":"XrSimpleText",
                    "content":"content"
                }
            ]
         }
    headers = {"Access-Token": "xxx"}
    rsp = requests.post(url, json=params, headers=headers)
    rsp_data = rsp.json()
    return rsp_data
​
```

---

**Java**

```java
public JSONObject updateSite() {
    String access_token = "xxx";
    final Long advertiser_id = 0L;  //操作的广告主id
    final String name = "site_name"
    Map bricks = new HashMap() {
        {     
            put("name","XrSimpleText");
            put("content","content");
        }
    }
    // 请求地址
    String open_api_url_prefix = "https://ad.oceanengine.com/open_api/2/";
    String uri = "tools/site/update/";
​
    // 请求参数
    Map data = new HashMap(){
        {
            put("advertiser_id",advertiser_id);
            put("name",name);
            put("bricks",bricks);
        }
    };
​
    // 构造请求
    HttpPost httpEntity = new HttpPost(open_api_url_prefix + uri);
​
    httpEntity.setHeader("Access-Token",access_token);
​
    CloseableHttpResponse response = null;
    CloseableHttpClient client = null;
​
    try {
        client = HttpClientBuilder.create().build();
        httpEntity.setEntity(new StringEntity(JSONObject.toJSONString(data), ContentType.APPLICATION_JSON));
​
        response = client.execute(httpEntity);
        if (response != null &amp;&amp; response.getStatusLine().getStatusCode() == 200) {
            BufferedReader bufferedReader  = new BufferedReader(new InputStreamReader(response.getEntity().getContent()));
            StringBuffer result = new StringBuffer();
            String line = "";
            while((line = bufferedReader.readLine()) != null) {
                result.append(line);
            }
            bufferedReader.close();
            return JSONObject.parseObject(result.toString());
        }
​
    } catch (ClientProtocolException e) {
        e.printStackTrace();
    } catch (IOException e) {
        e.printStackTrace();
    } finally {
        try {
            if (response != null) {
                response.close();
            }
            client.close();
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
    return null;
}
​
```

---

**Php**

```php
$client = new http\Client;
$request = new http\Client\Request;
​
$body = new http\Message\Body;
$body-&gt;append('{
    "advertiser_id": {{advertiser_id}},
    "name":"site_name",
    "bricks":[
                {
                    "name":"XrSimpleText",
                    "content":"content"
                }
            ]
    }');
​
$request-&gt;setRequestUrl('https://ad.oceanengine.com/open_api/2/tools/site/update/');
$request-&gt;setRequestMethod('POST');
$request-&gt;setBody($body);
​
$request-&gt;setHeaders(array(
    'Access-Token' =&gt; '{{access_token}}',
    'Content-Type' =&gt; 'application/json'
));
​
$client-&gt;enqueue($request)-&gt;send();
$response = $client-&gt;getResponse();
​
echo $response-&gt;getBody();
​
```

---

**Curl**

```bash
POST /open_api/2/tools/site/update/ HTTP/1.1 
Host: ad.oceanengine.com  
Content-Type: application/json  
Access-Token: xxx 
{
     "advertiser_id": 0,
     "name":"site_name",
     "bricks":[
        {
            "name":"XrSimpleText",
            "content":"content"
        }
    ]
}
​
```

### 应答字段

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| code | number | 返回码,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| message | string | 返回信息,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| data | json | json返回值，此处为`{}` |
| request\_id | string | 请求日志id |

### 应答示例

```
{
    "message": "OK",
    "code": 0,
    "data": {},
    "request_id": "110"
}
​
```

