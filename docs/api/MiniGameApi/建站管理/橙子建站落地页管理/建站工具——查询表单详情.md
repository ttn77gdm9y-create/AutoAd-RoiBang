通过此接口，用户可以根据表单id获取已有表单的信息详情。

> 详情包括客户ID、实例ID、实例名称、表单标题、提交按钮文案、副标题、表单元素、元素标签、是否允许为空、表单元素类型、是否可重复等信息。

关键信息

详情

是否支持SDK

支持

权限点及应用类型

了解如何 [申请授权&生成授权](https://open.oceanengine.com/labels/7/docs/1803016515293203?origin=metadata)

权限点:  工具-建站管理-获取站点

应用类型:  自研投放应用-客户/代理商/独立三方站点/群峰上架服务、提供第三方服务-客户/代理商/独立三方站点/群峰上架服务

接入能力范围:  巨量 PC

支持的账户类型

客户投放账户

##### 请求地址

<https://ad.oceanengine.com/open_api/2/tools/clue/form/detail/>
[可视化调试](https://open.oceanengine.com/tools/visual_debug.html?docId=1696710622245903)
[SDK下载](https://open.oceanengine.com/labels/7/docs/1773083358100557)
[MCP调用](https://open.oceanengine.com/labels/7/docs/1847394521222599)

##### 请求方法

GET

##### 请求Header

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| Access-Token必填 | string | 授权access-token，获取方法见接口文档[【获取Access-Token】](https://open.oceanengine.com/labels/7/docs/1696710505596940) |

##### 请求参数

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| advertiser\_id必填 | number | 客户id，数字的范围：1 <= advertiser\_id <= `MAX_INT64` |
| instance\_id必填 | number | 表单ID |

##### 请求示例

**Python**

```python
def demo():
  import requests
  uri = "https://ad.oceanengine.com/open_api/2/tools/clue/form/detail/"
  params={
        "advertiser_id": 0,
        "instance_id": 0,
   }
   headers = {"Access-Token": "xxx"}
   rsp = requests.get(url, json=params, headers=headers)
   rsp = json.loads(rsp.text)
   return rsp
​
```

---

**Java**

```java
def demo():
  import requests
  uri = "https://ad.oceanengine.com/open_api/2/tools/clue/form/detail/"
  params={
        "advertiser_id": 0,
        "instance_id": 0,
   }
   headers = {"Access-Token": "xxx"}
   rsp = requests.get(url, json=params, headers=headers)
   rsp = json.loads(rsp.text)
   return rsp
​
```

---

**Php**

```php
def demo():
  import requests
  uri = "https://ad.oceanengine.com/open_api/2/tools/clue/form/detail/"
  params={
        "advertiser_id": 0,
        "instance_id": 0,
   }
   headers = {"Access-Token": "xxx"}
   rsp = requests.get(url, json=params, headers=headers)
   rsp = json.loads(rsp.text)
   return rsp
​
```

---

**Curl**

```bash
GET /open_api/2/tools/clue/form/detail/ HTTP/1.1 
Host: ad.oceanengine.com  
Content-Type: application/json  
Access-Token: xxx 
{
     "advertiser_id": 0,
     "instance_id": 0
}
​
```

##### 应答参数

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| code | number | 返回码,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| message | string | 返回信息,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| data | json | json返回值 |
| advertiser\_id | number | 客户ID |
| instance\_id | number | 表单ID |
| name | string | 表单名 |
| title | string | 表单标题 |
| submit\_text | string | 提交按钮文案 |
| sub\_title | string | 副标题 |
| elements | object[] | 表单元素 |
| label | string | 元素标签 |
| element\_id | number | 表单项id |
| value | string | 表单项数据 |
| allow\_empty | number | 是否允许为空 返回值： `0`（必填）， `1`（可为空） |
| type | string | 表单元素类型 返回值： `NAME`（姓名） `TELEPHONE`（电话） `EMAIL`（邮箱） `NUMBER`（数值） `SEX`（性别） `DATE`（日期） `CITY`（城市） `TEXT`（文本） `TEXTAREA`（文本域） `SELECT`（下拉框） `RADIO`（单选框） `CHECKBOX`（多选框） `CALCULATOR`（计算器） |
| is\_unique | number | 是否可重复 返回值： `0`（唯一） `1`（可重复） |
| request\_id | string | 请求日志id |

##### 应答示例

```
{
    "message": "OK",
    "code": 0,
    "data": [
        {
            "instance_id": 123,
            "elements": [
                {
                    "element_id":123123123123,
                    "value":"",
                    "is_unique": 1,
                    "type": "TELEPHONE",
                    "allow_empty": 0,
                    "label": "电话"
                },
                {
                    "element_id":123123123124,
                    "value":"",
                    "is_unique": 0,
                    "type": "NAME",
                    "allow_empty": 0,
                    "label": "姓名"
                }
            ],
            "name": "xxx",
            "title": "",
            "submit_text": "xxxxxx",
            "sub_title": "",
            "advertiser_id": 11
        }
    ],
    "request_id": "202002141136590100100250740901C5B8"
}
​
```

