通过此接口，用户可以获取客户建站列表。

> 列表栏包括建站ID、建站名称、建站状态、建站类型、建站类别、站点缩略图等信息。

* 该接口当前还不会返回建站地址，建站地址可由如下格式拼装得到：<https://www.chengzijianzhan.com/tetris/page/XXXXXXXXXXXXX/（其中XX是建站ID，拼装后就可获得投放的URL/预览URL）>

关键信息

详情

是否支持SDK

支持

权限点及应用类型

了解如何 [申请授权&生成授权](https://open.oceanengine.com/labels/7/docs/1803016515293203?origin=metadata)

权限点:  工具-建站管理-获取站点

应用类型:  自研投放应用-客户/代理商/独立三方站点/群峰上架服务、提供第三方服务-客户/代理商/独立三方站点/群峰上架服务

接入能力范围:  巨量 PC

支持的账户类型

客户投放账户

### 请求地址

<https://ad.oceanengine.com/open_api/2/tools/site/get/>
[可视化调试](https://open.oceanengine.com/tools/visual_debug.html?docId=1696710620579852)
[SDK下载](https://open.oceanengine.com/labels/7/docs/1773083358100557)
[MCP调用](https://open.oceanengine.com/labels/7/docs/1847394521222599)

### 请求方法

GET

### 请求Header

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| Access-Token必填 | string | 授权access-token，获取方法见接口文档[【获取Access-Token】](https://open.oceanengine.com/labels/7/docs/1696710505596940) |

### 请求参数

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| advertiser\_id必填 | number | 客户id，范围：1 <= advertiser\_id <= `MAX_INT64` |
| page | int | 页码，默认值: `1`，范围：page >= 1 |
| page\_size | int | 页面数据量，默认值：10，范围：`20～300` |
| status | string | 建站粗粒度状态，详见[【附录-枚举值-建站粗粒度状态】](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710760171535#custom-anchor-建站粗颗粒度状态) |
| share\_type 新增 | string | 站点来源  `SHARE` 共享站点  `MY_CREATIONS`账户创建站点 (默认） |
| filtering | json | 过滤条件 |
| search | string | 搜索字段，按照建站id和建站name进行模糊匹配，范围：1 <= 长度 <= 50 |

### 请求示例

**Python**

```python
def get_site():
    import requests
    open_api_url_prefix = "https://ad.oceanengine.com/open_api/"
    uri = "1/tools/site/get/"
    url = open_api_url_prefix + uri
    params = {
        "advertiser_id": xxx,
        "page": 1,
        "page_size": 20,
    }
    headers = {"Access-Token": "xxx"}
    rsp = requests.get(url, json=params, headers=headers)
    rsp_data = rsp.json()
    return rsp_data
​
```

---

**Java**

```java
public JSONObject getSite() {
    String access_token = "xxx";
    final Long advertiser_id = 0L;
    final int page = 1;
    final int page_size = 10;
    // 请求地址
    String open_api_url_prefix = "https://ad.oceanengine.com/open_api/2/";
    String uri = "tools/site/get/";
​
    // 请求参数
    Map data = new HashMap(){
        {
            put("advertiser_id",advertiser_id);
            put("page", page);
            put("page_size", page_size);
        }
    };
​
    // 构造请求
    HttpEntityEnclosingRequestBase httpEntity = new HttpEntityEnclosingRequestBase() {
        @Override
        public String getMethod() {
            return "GET";
        }
    };
​
    httpEntity.setHeader("Access-Token",access_token);
​
    CloseableHttpResponse response = null;
    CloseableHttpClient client = null;
​
    try {
        client = HttpClientBuilder.create().build();
        httpEntity.setURI(URI.create(open_api_url_prefix + uri));
        httpEntity.setEntity(new StringEntity(JSONObject.toJSONString(data), ContentType.APPLICATION_JSON));
​
        response = client.execute(httpEntity);
        if (response != null && response.getStatusLine().getStatusCode() == 200) {
            BufferedReader bufferedReader  = new BufferedReader(new InputStreamReader(response.getEntity().getContent()));
            StringBuffer result = new StringBuffer();
            String line = "";
            while((line = bufferedReader.readLine()) != null) {
                result.append(line);
            }
            bufferedReader.close();
            return JSONObject.parseObject(result.toString());
        }
​
    } catch (ClientProtocolException e) {
        e.printStackTrace();
    } catch (IOException e) {
        e.printStackTrace();
    } finally {
        try {
            if (response != null) {
                response.close();
            }
            client.close();
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
    return null;
}
​
```

---

**Php**

```php
$client = new http\Client;
$request = new http\Client\Request;
​
$body = new http\Message\Body;
$body->append('{
   "advertiser_id":{{advertiser_id}}
}');
​
$request->setRequestUrl('https://ad.oceanengine.com/open_api/2/tools/site/get/');
$request->setRequestMethod('GET');
$request->setBody($body);
​
$request->setHeaders(array(
    'Access-Token' => '{{access_token}}',
    'Content-Type' => 'application/json'
));
​
$client->enqueue($request)->send();
$response = $client->getResponse();
​
echo $response->getBody();
​
```

---

**Curl**

```bash
curl -G https://ad.oceanengine.com/open_api/2/tools/site/get/ \
    -H "Access-Token: xxx" \
    -d "advertiser_id=xxx" \
    -d "page=1" \
    -d "page_size=20"
​
```

### 应答字段

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| code | number | 返回码,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| message | string | 返回信息,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| data | json | json返回值 |
| page\_info | object | 分页信息 |
| page | number | 页码 |
| page\_size | number | 页面大小 |
| total\_number | number | 总数 |
| total\_page | number | 总页数 |
| list | list | 建站列表 |
| siteId | string | 建站id |
| name | string | 建站名称 |
| status | string | 建站状态，详见附录[【附录-枚举值-建站状态】](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710760171535#custom-anchor-建站状态) |
| siteType | string | 建站类型，详见附录[【附录-枚举值-建站类型】](https://ad.oceanengine.com/openapi/doc/index.html?id=1696710760171535#custom-anchor-建站类型) |
| function\_type | string | 建站类别， `SITE_FUNC_TYPE_NEW_MODULAR`（智能建站） `SITE_FUNC_TYPE_NORMAL`（普通建站） |
| thumbnail | string | 站点缩略图 |
| request\_id | string | 请求日志id |

### 应答示例

```
HTTPS/1.1 200 OK
{
    "message": "OK",
    "code": 0,
    "data": {
        "list": [
            {
                "siteId": "xxx",
                "name": "xxx",
                "status": 3,
                "siteType": "0",
                "function_type": "SITE_FUNC_TYPE_NEW_MODULAR",
                "thumbnail": "https://tos.pstatp.com/xxx~noop.jpeg"
            }
        ],
        "page_info": {
            "page": 1,
            "page_size": 20,
            "total_page": 1,
            "total_count": 20
        }
    }
}
​
```

