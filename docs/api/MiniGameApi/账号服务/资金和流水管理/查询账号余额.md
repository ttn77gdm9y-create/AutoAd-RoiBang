获取客户或代理商账户余额信息。

关键信息

详情

关联的消息订阅能力

SPI: 账户余额变动|[account.balance.change](https://open.oceanengine.com/labels/7/docs/1722468664901647?origin=metadata)

是否支持SDK

支持

权限点及应用类型

了解如何 [申请授权&生成授权](https://open.oceanengine.com/labels/7/docs/1803016515293203?origin=metadata)

权限点:  账号服务-资金和流水管理-查询资金与流水

应用类型:  自研投放应用-客户/代理商/独立三方站点/群峰上架服务、提供第三方服务-客户/代理商/独立三方站点/群峰上架服务

接入能力范围:  巨量 PC

支持的账户类型

客户投放账户

代理商账户

巨量引擎工作台账户

星图客户

### 请求地址

<https://ad.oceanengine.com/open_api/2/advertiser/fund/get/>
[可视化调试](https://open.oceanengine.com/tools/visual_debug.html?docId=1696710526192652)
[SDK下载](https://open.oceanengine.com/labels/7/docs/1773083358100557)
[MCP调用](https://open.oceanengine.com/labels/7/docs/1847394521222599)

### 请求方法

GET

### 请求Header

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| Access-Token 必填 | string | 授权access-token，获取方法见接口文档[【获取Access-Token】](https://open.oceanengine.com/labels/7/docs/1696710505596940) |

### 请求参数

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| advertiser\_id 必填 | number | 客户ID或代理商ID |
| grant\_type\_split | string | 是否拆分赠款类型，允许值：  开启 `ON` ，关闭 `OFF`（默认） |

### 请求示例

**Python**

```python
def advertiser_fund_get():
    import requests
    open_api_url_prefix = "https://ad.oceanengine.com/open_api/"
    uri = "2/advertiser/fund/get/"
    url = open_api_url_prefix + uri
    params = {
        "advertiser_id": 0
    }
    headers = {"Access-Token": "xxx"}
    rsp = requests.get(url, json=params, headers=headers)
    rsp_data = rsp.json()
    return rsp_data
```

---

**Java**

```java
public static JSONObject getAdvertiserFund() {
    String access_token = "xxx";
    final Long advertiser_id = 0L;
​
    // 请求地址
    String open_api_url_prefix = "https://ad.oceanengine.com/open_api/2/";
    String uri = "advertiser/fund/get/";
​
    // 请求参数
    Map data = new HashMap(){
        {
            put("advertiser_id", advertiser_id);
        }
    };
​
    // 构造请求
    HttpEntityEnclosingRequestBase httpEntity = new HttpEntityEnclosingRequestBase() {
        @Override
        public String getMethod() {
            return "GET";
        }
    };
​
    httpEntity.setHeader("Access-Token",access_token);
​
    CloseableHttpResponse response = null;
    CloseableHttpClient client = null;
​
    try {
        client = HttpClientBuilder.create().build();
        httpEntity.setURI(URI.create(open_api_url_prefix + uri));
        httpEntity.setEntity(new StringEntity(JSONObject.toJSONString(data), ContentType.APPLICATION_JSON));
​
        response = client.execute(httpEntity);
        if (response != null &amp;&amp; response.getStatusLine().getStatusCode() == 200) {
            BufferedReader bufferedReader  = new BufferedReader(new InputStreamReader(response.getEntity().getContent()));
            StringBuffer result = new StringBuffer();
            String line = "";
            while((line = bufferedReader.readLine()) != null) {
                result.append(line);
            }
            bufferedReader.close();
            return JSONObject.parseObject(result.toString());
        }
​
    } catch (ClientProtocolException e) {
        e.printStackTrace();
    } catch (IOException e) {
        e.printStackTrace();
    } finally {
        try {
            if (response != null) {
                response.close();
            }
            client.close();
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
    return null;
}
```

---

**Php**

```php
$client = new http\Client;
$request = new http\Client\Request;
​
$body = new http\Message\Body;
$body-&gt;append('{
   "advertiser_id": xxxxxxxxxx
}  ');
​
$request-&gt;setRequestUrl('https://ad.oceanengine.com/open_api/2/advertiser/fund/get/');
$request-&gt;setRequestMethod('GET');
$request-&gt;setBody($body);
​
$request-&gt;setHeaders(array(
  'Access-Token' =&gt; 'xxxxxxxxxx',
  'Content-Type' =&gt; 'application/json'
));
​
$client-&gt;enqueue($request)-&gt;send();
$response = $client-&gt;getResponse();
​
echo $response-&gt;getBody();
```

---

**Curl**

```bash
GET /open_api/2/advertiser/fund/get/ HTTP/1.1
Host: ad.oceanengine.com
Content-Type: application/json
Access-Token: xxx
{
    "advertiser_id": 0
}
```

### 应答参数

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| code | number | 返回码,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| message | string | 返回信息,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| data | json | json返回值 |
| advertiser\_id | number | 客户ID或代理商ID |
| name | string | 账户名 |
| email | string | 联系邮箱 |
| balance | number | 账户总余额(单位元) |
| valid\_balance | number | 账户可用总余额(单位元) |
| cash | number | 现金余额(单位元) |
| valid\_cash | number | 现金可用余额(单位元) |
| grant | number | 赠款余额(单位元) |
| default\_grant | float | 默认投放版位可用赠款余额(单位元) |
| common\_grant | float | 通用投放版位可用赠款余额(单位元) |
| search\_grant | float | 搜索投放版位可用赠款余额(单位元) |
| union\_grant | float | 穿山甲投放版位可用赠款余额(单位元) |
| valid\_grant | number | 赠款可用余额(单位元) |
| return\_goods\_abs | number | 返货余额(单位元)，仅支持部分客户 |
| valid\_return\_goods\_abs | number | 返货可用余额(单位元)，仅支持部分客户 |
| return\_goods\_cost | number | 返货支出(单位元)，仅支持部分客户 |
| return\_goods\_grant | number | 返货赠款 |
| compensation\_grant | number | 赔付赠款 |
| return\_goods\_valid\_grant | number | 返货可用赠款 |
| compensation\_valid\_grant | number | 赔付可用赠款 |
| wallet\_id | string | 钱包id（投放账户绑定的共享子钱包id） |
| wallet\_name | string | 钱包名称（投放账户绑定的共享子钱包名称） |
| wallet\_total\_balance\_valid | number | 账户绑定的子钱包的可用共享余额（单位元） |
| request\_id | string | 请求日志id |

### 应答示例

```
HTTPS/1.1 200 OK
{
    {
        "message": "OK",
        "code": 0,
        "data": {
            "advertiser_id": xxx,
            "name": "xxx",
            "emal": "xxx",
            "balance": 0,
            "valid_balance": 0,
            "cash": 0,
            "valid_cash": 0,
            "grant": 0,
            "union_grant": 0,
            "search_grant": 0,
            "common_grant": 0,
            "default_grant": 0,
            "valid_grant": 0
        },
        "request_id": "×××"
    }
}
```

