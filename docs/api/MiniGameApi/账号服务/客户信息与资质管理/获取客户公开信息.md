获取投放账户基础信息，无需申请权限

关键信息

详情

是否支持SDK

支持

支持的账户类型

客户投放账户

本地推

# 请求地址

<https://ad.oceanengine.com/open_api/2/advertiser/public_info/>
[可视化调试](https://open.oceanengine.com/tools/visual_debug.html?docId=1696710511950860)
[SDK下载](https://open.oceanengine.com/labels/7/docs/1773083358100557)
[MCP调用](https://open.oceanengine.com/labels/7/docs/1847394521222599)

# 请求方式

**GET**

# 请求Header

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| Access-Token 必填 | string | 授权access-token，获取方法见接口文档[【获取Access-Token】](https://open.oceanengine.com/labels/7/docs/1696710505596940) |

# 请求参数

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| advertiser\_ids 必填 | number[] | 客户ID集合(如果包含没有访问权限的ID,将返回PERMISSION\_ERROR).取值范围: `1-100` |

# 请求示例

**Python**

```python
def get_advertiser_info():
    import requests
    open_api_url_prefix = "https://ad.toutiao.com/open_api/"
    uri = "2/advertiser/public_info/"
    url = open_api_url_prefix + uri
    params = {
        "advertiser_ids": [0]
    }
    headers = {"Access-Token": "xxx"}
    rsp = requests.get(url, json=params, headers=headers)
    rsp_data = rsp.json()
    return rsp_data
```

---

**Java**

```java
public static JSONObject getAdvertiserPublicInfo() {
    String access_token = "xxx";
    final Long advertiser_id = 0L;
​
    // 请求地址
    String open_api_url_prefix = "https://ad.oceanengine.com/open_api/2/";
    String uri = "advertiser/public_info/";
​
    // 请求参数
    Map data = new HashMap(){
        {
            put("advertiser_ids", new Long[] {advertiser_id});
        }
    };
​
    // 构造请求
    HttpEntityEnclosingRequestBase httpEntity = new HttpEntityEnclosingRequestBase() {
        @Override
        public String getMethod() {
            return "GET";
        }
    };
​
    httpEntity.setHeader("Access-Token",access_token);
​
    CloseableHttpResponse response = null;
    CloseableHttpClient client = null;
​
    try {
        client = HttpClientBuilder.create().build();
        httpEntity.setURI(URI.create(open_api_url_prefix + uri));
        httpEntity.setEntity(new StringEntity(JSONObject.toJSONString(data), ContentType.APPLICATION_JSON));
​
        response = client.execute(httpEntity);
        if (response != null && response.getStatusLine().getStatusCode() == 200) {
            BufferedReader bufferedReader  = new BufferedReader(new InputStreamReader(response.getEntity().getContent()));
            StringBuffer result = new StringBuffer();
            String line = "";
            while((line = bufferedReader.readLine()) != null) {
                result.append(line);
            }
            bufferedReader.close();
            return JSONObject.parseObject(result.toString());
        }
​
    } catch (ClientProtocolException e) {
        e.printStackTrace();
    } catch (IOException e) {
        e.printStackTrace();
    } finally {
        try {
            if (response != null) {
                response.close();
            }
            client.close();
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
    return null;
}
```

---

**Php**

```php
$client = new http\Client;
$request = new http\Client\Request;
​
$body = new http\Message\Body;
$body->append('{
   "advertiser_ids": [xxxxxxxxxx]
}  ');
​
$request->setRequestUrl('https://ad.toutiao.com/open_api/2/advertiser/public_info/');
$request->setRequestMethod('GET');
$request->setBody($body);
​
$request-&gt;setHeaders(array(
  'Access-Token' => 'xxxxxxxxxx',
  'Content-Type' => 'application/json'
));
​
$client->enqueue($request)->send();
$response = $client->getResponse();
​
echo $response->getBody();
```

---

**Curl**

```bash
GET /open_api/2/advertiser/public_info/ HTTP/1.1
Host: ad.oceanengine.com
Content-Type: application/json
Access-Token: xxx
{
    "advertiser_ids": [0]
}
```

# 应答参数

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| code | number | 返回码,详见[【附录-返回码】](https://open.oceanengine.com/labels/12/docs/1699633774328846) |
| message | string | 返回信息,详见[【附录-返回码】](https://open.oceanengine.com/labels/12/docs/1699633774328846) |
| data | object[] | 客户列表 |
| id | number | 账户ID |
| name | string | 账户名 |
| company | string | 公司名 |
| first\_industry\_name | string | 一级行业名 |
| second\_industry\_name | string | 二级行业名 |
| request\_id | string | 请求日志ID |

# 应答示例

```
HTTPS/1.1 200 OK
{
    "message": "OK",
    "code": 0,
    "data": [
        {
            "id": 0,
            "name": "xxx",
            "company": "xxx",
            "first_industry_name": "xxx",
            "second_industry_name": "xxx"
        }
    ],
    "request_id": "xxx"
}
```

