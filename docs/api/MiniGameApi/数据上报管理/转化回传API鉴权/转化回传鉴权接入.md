## 接入流程

客户切换到鉴权回传转化有3个步骤，分别是新增公钥、测试鉴权、开启鉴权。

1. 新增公钥。客户将公钥保存到平台。为确保客户使用了正确的公钥格式和签名算法，在新增公钥时有一步验签的动作，客户使用私钥对指定文本（见下文）签名。
2. 测试鉴权。在新增公钥之后，客户即可对转化回传API的鉴权功能进行测试。鉴权关闭状态下，鉴权失败的转化不会被丢弃，同时返回鉴权失败原因，方便客户调试定位。
3. 开启鉴权。在客户完成代码上线，并确认上线后有成功回传转化，且没有鉴权失败（观察24h以上，最长不超过一周）之后，可请求“开启鉴权”接口，平台在检查满足条件后开启鉴权。开启鉴权后，鉴权失败的转化数据将被丢弃。

![](https://p26-business-sign.byteadimg.com/tos-cn-i-yvqzo4lhg5/fbdf4942e350428380b654bf36fdf8eb~noop.image?x-expires=1988790215&x-signature=GOcWzBTIe3UqTKdIsGN15eofw4s%3D)

## 鉴权方案

使用RS256 (RSASSA-PKCS1-v1\_5 using SHA-256) 数字签名算法，以保证消息的完整性、进行认证以及防止否认。

### 必备条件

* PrivateKey/PublicKey: 密钥对，用于生成签名和验签
* credential: 密钥对label

必须首先通过“新增公钥”接口将PublicKey及对应的credential保存到平台。

用于鉴权的HTTP请求头：

```
x-rs256-token: credential=<value>×tamp=<value>&signature=<value>
```

| **字段** | **类型** | **是否必选** | **说明** |
| --- | --- | --- | --- |
| credential | string | 是 | 密钥对label，枚举值："primary"或"backup"。 |
| timestamp | long | 是 | 发出请求的Unix时间戳，单位为秒。它与当前时间相差不能超过 15 分钟。 |
| signature | string | 是 | 生成的签名，计算方法见后文。 |

示例：

```
x-rs256-token: credential=primary×tamp=1633766480&signature=vY34PBLBvO07riX2suUHZWhjTw1lQQTbfLiKkmnfEvoQHeuaY2cxomRJkrCQrf1X9A7eKkikLzavxX4+V1QW6qhprAuxOEnA9Jpk9uQY0c18wx5rkiZM4PCIyCFBD6zVpnm4use0QX5lM025EyT08ycInCpraaROXOUKo9EpsH0=
```

### 计算signature

使用RS256算法对StringToSign计算签名，签名结果使用标准Base64编码。

StringToSign格式如下：

```
StringToSign=
HTTP_METHOD + '\n' + path_and_query + '\n' + timestamp + '\n' + content_sha256
```

| **字段** | **说明** |
| --- | --- |
| HTTP\_METHOD | 请求所使用的大写HTTP方法名。 |
| path\_and\_query | 串联在一起的请求绝对URI路径和查询字符串。path末尾若没有'/'，需要加上'/'。注意：拼接的query部分需要进行URL编码。 |
| timestamp | 发出请求的Unix时间戳，单位为秒。它与当前时间相差不能超过 15 分钟。 |
| content\_sha256 | 请求正文的标准 base64 编码的 SHA256 哈希。 即使没有正文，也必须提供它。 base64\_standard\_encoding(SHA256(body)) |

示例：

```
StringToSign=
            "GET" + '\n' +                                 // VERB
            "/track/activate/?event_type=324&callback=CNXUIhCpEhgAOABCFTEyMzEzMTIzMTJqbGtqa2wxNDMxM5ABAA==" + '\n' +                     // path_and_query
            "1633766480" + '\n' +                          // timestamp
            "8ppEi3gHRb8uEGZ/RsRCsQLnXnakah//lpZBhmIlq1Y=" // content_sha256
```

RS256签名示例：

```
String rs256(PrivateKey privateKey, String stringToSign) throws Exception {
    final Signature s = Signature.getInstance("SHA256withRSA");
    s.initSign(privateKey);
    s.update(stringToSign.getBytes(StandardCharsets.UTF_8));
    byte[] sig = s.sign();
    String signature = Base64.getEncoder().encodeToString(sig);
    return signature;
}
```

### 错误

| code | message | 描述 |
| --- | --- | --- |
| 0 | OK | 成功 |
| 16 | 具体msg见返回信息 | 鉴权错误 |

错误信息示例

```
// 鉴权请求头未提供
auth failed, the auth token is not provided, please provide a valid auth token header: x-rs256-token
​
// 鉴权时间戳过期
auth failed, the auth token has expired (expiration time is 15 minutes), please regenerate a new one
​
// 鉴权时间戳格式非法
auth failed, the timestamp format is invalid, please use unix timestamp in seconds
​
// 鉴权请求头缺失必要参数
auth failed, missing a required parameter from the auth token header, please provide the three parameters (credential, timestamp, signature) in auth token header: x-rs256-token
​
// 鉴权使用的广告主公钥未添加
auth failed, the provided credential is not found, please make sure the public key has been added
​
// 鉴权生成的签名校验失败
auth failed, the provided signature is invalid, please make sure the signature is generated correctly
```

### 代码示例

Java

```
package org.example;
​
import org.apache.commons.codec.digest.DigestUtils;
import org.apache.commons.io.IOUtils;
import org.apache.http.HttpEntityEnclosingRequest;
import org.apache.http.HttpResponse;
import org.apache.http.client.methods.HttpGet;
import org.apache.http.client.methods.HttpPost;
import org.apache.http.client.methods.HttpUriRequest;
import org.apache.http.client.utils.URIBuilder;
import org.apache.http.entity.ContentType;
import org.apache.http.entity.StringEntity;
import org.apache.http.impl.client.CloseableHttpClient;
import org.apache.http.impl.client.HttpClients;
import org.apache.http.util.EntityUtils;
import org.bouncycastle.util.io.pem.PemObject;
import org.bouncycastle.util.io.pem.PemReader;
​
import java.io.*;
import java.net.URI;
import java.nio.charset.StandardCharsets;
import java.security.KeyFactory;
import java.security.NoSuchAlgorithmException;
import java.security.PrivateKey;
import java.security.Signature;
import java.security.spec.EncodedKeySpec;
import java.security.spec.InvalidKeySpecException;
import java.security.spec.PKCS8EncodedKeySpec;
import java.time.Instant;
import java.util.Base64;
​
public class Sign {
​
    public static void main(String[] args) throws Exception {
        // 转化事件回调服务 HTTP GET demo https://open.oceanengine.com/doc/index.html?key=ad&type=api&id=1696710656359439
        trackActivateGetDemo();
​
        // 转化事件回调服务 HTTP POST demo https://open.oceanengine.com/doc/index.html?key=ad&type=api&id=1696710656359439
        trackActivatePostDemo();
​
        // 转化回传 API 对接说明 https://event-manager.oceanengine.com/docs/8650/app_api_docs/
        conversionDemo();
    }
​
    private static void trackActivateGetDemo() throws Exception {
        URIBuilder builder = new URIBuilder("https://ad.oceanengine.com/track/activate/")
                .setParameter("event_type", "324")
                .setParameter("callback", "CNXUIhCpEhgAOABCFTEyMzEzMTIzMTJqbGtqa2wxNDMxM5ABAA==")
                .setParameter("event_weight", "0")
                .setParameter("conv_time", "1635841206463")
                .setParameter("props", "{\"consumption\":2106}");
​
        HttpGet request = new HttpGet(builder.build());
​
        // 计算x-rs256-token
        String token = calcToken(request, "primary");
        request.setHeader("x-rs256-token", token);
        System.out.println("x-rs256-token: " + token);
​
        execute(request);
    }
​
    private static void trackActivatePostDemo() throws Exception {
        HttpPost request = new HttpPost("https://ad.oceanengine.com/track/activate/");
​
        StringEntity requestEntity = new StringEntity(
                "{\n" +
                        "    \"event_type\": 324,\n" +
                        "    \"callback\": \"CNXUIhCpEhgAOABCFTEyMzEzMTIzMTJqbGtqa2wxNDMxM5ABAA==\",\n" +
                        "    \"event_weight\": 0,\n" +
                        "    \"conv_time\": 1635841206463\n" +
                        "}",
                ContentType.APPLICATION_JSON);
​
        request.setHeader("Content-Type", "application/json");
​
        request.setEntity(requestEntity);
​
        // 计算x-rs256-token
        String token = calcToken(request, "primary");
        request.setHeader("x-rs256-token", token);
        System.out.println("x-rs256-token: " + token);
​
        execute(request);
    }
​
    private static void conversionDemo() throws Exception {
        HttpPost request = new HttpPost("https://analytics.oceanengine.com/api/v2/conversion");
​
        StringEntity requestEntity = new StringEntity(
                "{\n" +
                        "    \"event_type\": \"active\",\n" +
                        "    \"event_weight\": 0.21312,\n" +
                        "    \"context\": {\n" +
                        "        \"ad\": {\n" +
                        "            \"callback\": \"CNXUIhCpEhgAOABCFTEyMzEzMTIzMTJqbGtqa2wxNDMxM5ABAA==\",\n" +
                        "            \"match_type\": 3\n" +
                        "        },\n" +
                        "        \"device\": {\n" +
                        "            \"platform\": \"ios\",\n" +
                        "            \"idfa\": \"FCD369C3-F622-44B8-AFDE-12065659F34B\"\n" +
                        "        }\n" +
                        "    },\n" +
                        "    \"properties\": {\n" +
                        "        \"pay_amount\": 1231.211\n" +
                        "    },\n" +
                        "    \"timestamp\": 1604888786102\n" +
                        "}",
                ContentType.APPLICATION_JSON);
​
        request.setEntity(requestEntity);
​
        request.setHeader("Content-Type", "application/json");
​
        // 计算x-rs256-token
        String token = calcToken(request, "primary");
        request.setHeader("x-rs256-token", token);
        System.out.println("x-rs256-token: " + token);
​
        execute(request);
    }
​
    // 发送请求
    private static void execute(HttpUriRequest request) throws IOException {
        try (CloseableHttpClient http = HttpClients.createDefault()) {
            HttpResponse response = http.execute(request);
            System.out.println(EntityUtils.toString(response.getEntity()));
        }
    }
​
    /**
     * 计算x-rs256-token
     *
     * @param request    http 请求
     * @param credential 密钥 credential (label：取值为"primary"或"backup")
     * @return x-rs256-token值
     * @throws Exception on exception
     */
    private static String calcToken(HttpUriRequest request, String credential) throws Exception {
        // get stringToSign
        String methodName = request.getRequestLine().getMethod().toUpperCase();
        URI uri = request.getURI();
        String pathAndQuery = uri.getPath();
        // 在path末尾加上'/'
        if (pathAndQuery == null) {
            pathAndQuery = "/";
        } else if (!pathAndQuery.endsWith("/")) {
            pathAndQuery += "/";
        }
        if (uri.getRawQuery() != null) {
            pathAndQuery = pathAndQuery + "?" + uri.getRawQuery();
        }
        long timestamp = Instant.now().getEpochSecond();
        String contentHash = buildContentHash(request);
        String stringToSign = String.format("%s\n%s\n%s\n%s", methodName, pathAndQuery, timestamp, contentHash);
        // System.out.println("stringToSign: " + stringToSign);
​
        // get signature
        String keyFile = String.format("./src/main/resources/pkcs8-%s.pem", credential);
        PrivateKey privateKey = PemUtils.readPrivateKeyFromFile(keyFile, "RSA");
        String signature = rs256(privateKey, stringToSign);
​
        // x-rs256-token: credential=<value>×tamp=<value>&signature=<value>
        return String.format("credential=%s×tamp=%s&signature=%s", credential, timestamp, signature);
    }
​
    static class PemUtils {
​
        public static PrivateKey readPrivateKeyFromFile(String filepath, String algorithm) throws IOException {
            byte[] bytes = parsePemFile(new File(filepath));
            return PemUtils.getPrivateKey(bytes, algorithm);
        }
​
        private static byte[] parsePemFile(File pemFile) throws IOException {
            if (!pemFile.isFile() || !pemFile.exists()) {
                throw new FileNotFoundException(String.format("The file '%s' doesn't exist.", pemFile.getAbsolutePath()));
            }
            PemReader reader = new PemReader(new FileReader(pemFile));
            PemObject pemObject = reader.readPemObject();
            byte[] content = pemObject.getContent();
            reader.close();
            return content;
        }
​
        private static PrivateKey getPrivateKey(byte[] keyBytes, String algorithm) {
            PrivateKey privateKey = null;
            try {
                KeyFactory kf = KeyFactory.getInstance(algorithm);
                EncodedKeySpec keySpec = new PKCS8EncodedKeySpec(keyBytes);
                privateKey = kf.generatePrivate(keySpec);
            } catch (NoSuchAlgorithmException e) {
                System.out.println("Could not reconstruct the private key, the given algorithm could not be found.");
            } catch (InvalidKeySpecException e) {
                System.out.println("Could not reconstruct the private key");
            }
​
            return privateKey;
        }
    }
​
    /**
     * 计算请求体 sha-256 hash
     *
     * @param request http 请求
     * @return 请求体 sha-256 hash
     * @throws IOException on exception
     */
    private static String buildContentHash(HttpUriRequest request) throws IOException {
        String content = "";
        if (request instanceof HttpEntityEnclosingRequest) {
            try {
                StringWriter writer = new StringWriter();
                IOUtils.copy(((HttpEntityEnclosingRequest) request).getEntity().getContent(), writer,
                        StandardCharsets.UTF_8);
​
                content = writer.toString();
            } finally {
                ((HttpEntityEnclosingRequest) request).getEntity().getContent().close();
            }
        }
​
        byte[] digest = new DigestUtils("SHA-256").digest(content);
        return Base64.getEncoder().encodeToString(digest);
    }
​
    /**
     * 计算 rs256 签名
     *
     * @param privateKey   私钥
     * @param stringToSign 要签名的字符串
     * @return 签名
     * @throws Exception on exception
     */
    private static String rs256(PrivateKey privateKey, String stringToSign) throws Exception {
        final Signature s = Signature.getInstance("SHA256withRSA");
        s.initSign(privateKey);
        s.update(stringToSign.getBytes(StandardCharsets.UTF_8));
        byte[] sig = s.sign();
        return Base64.getEncoder().encodeToString(sig);
    }
​
}
```

Golang

```
package main
​
import (
        "bytes"
        "crypto"
        "crypto/rand"
        "crypto/rsa"
        "crypto/sha256"
        "crypto/x509"
        "encoding/base64"
        "encoding/pem"
        "fmt"
        "io/ioutil"
        "log"
        "net/http"
        "strings"
        "time"
)
​
func main() {
        // 转化事件回调服务 HTTP GET demo https://open.oceanengine.com/doc/index.html?key=ad&type=api&id=1696710656359439
        trackActivateGetDemo()
​
        // 转化事件回调服务 HTTP POST demo https://open.oceanengine.com/doc/index.html?key=ad&type=api&id=1696710656359439
        trackActivatePostDemo()
​
        // 转化回传 API 对接说明 https://event-manager.oceanengine.com/docs/8650/app_api_docs/
        conversionDemo()
}
​
func trackActivateGetDemo() {
        url := "https://ad.oceanengine.com/track/activate/"
        req, err := http.NewRequest(http.MethodGet, url, nil)
​
        q := req.URL.Query()
        q.Add("event_type", "324")
        q.Add("callback", "CNXUIhCpEhgAOABCFTEyMzEzMTIzMTJqbGtqa2wxNDMxM5ABAA==")
        q.Add("event_weight", "0")
        q.Add("conv_time", "1635841206463")
        q.Add("props", "{\"consumption\":2106}")
​
        req.URL.RawQuery = q.Encode()
​
        // 计算x-rs256-token
        token, err := calcToken(req, "primary")
        if err != nil {
                log.Fatal(err)
        }
        req.Header.Add("x-rs256-token", token)
        log.Println("x-rs256-token:", token)
​
        execute(req)
}
​
func trackActivatePostDemo() {
        var jsonStr = []byte(`{
    "event_type": 324,
    "callback": "CNXUIhCpEhgAOABCFTEyMzEzMTIzMTJqbGtqa2wxNDMxM5ABAA==",
    "event_weight": 0,
    "conv_time": 1635841206463
}`)
        req, _ := http.NewRequest("POST", "https://ad.oceanengine.com/track/activate/", bytes.NewBuffer(jsonStr))
​
        req.Header.Add("Content-Type", "application/json")
​
        // 计算x-rs256-token
        token, err := calcToken(req, "primary")
        if err != nil {
                log.Fatal(err)
        }
        req.Header.Add("x-rs256-token", token)
        log.Println("x-rs256-token:", token)
​
        execute(req)
}
​
func conversionDemo() {
        var jsonStr = []byte(`{
    "event_type": "active",
    "event_weight": 0.21312,
    "context": {
        "ad": {
            "callback": "CNXUIhCpEhgAOABCFTEyMzEzMTIzMTJqbGtqa2wxNDMxM5ABAA==",
            "match_type": 3
        },
        "device": {
            "platform": "ios",
            "idfa": "FCD369C3-F622-44B8-AFDE-12065659F34B"
        }
    },
    "properties": {
        "pay_amount": 1231.211
    },
    "timestamp": 1604888786102
}`)
        req, _ := http.NewRequest("POST", "https://analytics.oceanengine.com/api/v2/conversion", bytes.NewBuffer(jsonStr))
​
        req.Header.Add("Content-Type", "application/json")
​
        // 计算x-rs256-token
        token, err := calcToken(req, "primary")
        if err != nil {
                log.Fatal(err)
        }
        req.Header.Add("x-rs256-token", token)
        log.Println("x-rs256-token:", token)
​
        execute(req)
}
​
// 发送请求
func execute(req *http.Request) {
        var client http.Client
        resp, err := client.Do(req)
        if err != nil {
                log.Fatal(err)
        }
        defer resp.Body.Close()
​
        bodyBytes, err := ioutil.ReadAll(resp.Body)
        if err != nil {
                log.Fatal(err)
        }
        bodyString := string(bodyBytes)
        log.Println(bodyString)
}
​
// calcToken 计算x-rs256-token
// credential为密钥label：取值为"primary"或"backup"
func calcToken(req *http.Request, credential string) (string, error) {
        // get stringToSign
        method := req.Method
        pathAndQuery := req.URL.Path
        // 在path末尾加上'/'
        if pathAndQuery == "" || pathAndQuery[len(pathAndQuery)-1] != '/' {
                pathAndQuery = pathAndQuery + "/"
        }
        if req.URL.RawQuery != "" {
                pathAndQuery = pathAndQuery + "?" + req.URL.RawQuery
        }
​
        timestamp := time.Now().Unix()
​
        var content []byte
​
        if req.Body != nil {
                body, err := ioutil.ReadAll(req.Body)
                if err != nil {
                        return "", err
                }
                req.Body = ioutil.NopCloser(bytes.NewBuffer(body))
​
                content = body
        }
​
        contentHash := getContentHashBase64(content)
​
        stringToSign := fmt.Sprintf("%v\n%v\n%v\n%v", strings.ToUpper(method), pathAndQuery, timestamp, contentHash)
​
        // get signature
        keyFile := fmt.Sprintf("./src/main/resources/pkcs8-%v.pem", credential)
        privateKey, err := readPrivateKeyFromFile(keyFile)
        if err != nil {
                return "", err
        }
​
        rng := rand.Reader
​
        hashed := sha256.Sum256([]byte(stringToSign))
​
        sigBytes, err := rsa.SignPKCS1v15(rng, privateKey, crypto.SHA256, hashed[:])
        if err != nil {
                return "", err
        }
        signature := base64.StdEncoding.EncodeToString(sigBytes)
​
        return fmt.Sprintf("credential=%v×tamp=%v&signature=%v", credential, timestamp, signature), nil
}
​
func getContentHashBase64(content []byte) string {
        hasher := sha256.New()
        hasher.Write(content)
        return base64.StdEncoding.EncodeToString(hasher.Sum(nil))
}
​
func readPrivateKeyFromFile(filepath string) (*rsa.PrivateKey, error) {
        content, err := ioutil.ReadFile(filepath)
        if err != nil {
                return nil, err
        }
​
        return parseRSAPrivateKeyFromPEM(content)
}
​
// parseRSAPrivateKeyFromPEM Parse PEM encoded PKCS1 or PKCS8 public key
func parseRSAPrivateKeyFromPEM(key []byte) (*rsa.PrivateKey, error) {
        var err error
​
        // Parse PEM block
        var block *pem.Block
        if block, _ = pem.Decode(key); block == nil {
                return nil, err
        }
​
        // Parse the key
        var parsedKey interface{}
        if parsedKey, err = x509.ParsePKCS8PrivateKey(block.Bytes); err != nil {
                return nil, err
        }
​
        var pkey *rsa.PrivateKey
        var ok bool
        if pkey, ok = parsedKey.(*rsa.PrivateKey); !ok {
                return nil, fmt.Errorf("not *rsa.PrivateKey type")
        }
​
        return pkey, nil
}
```

### 参考

1. <https://docs.microsoft.com/en-us/azure/azure-app-configuration/rest-api-authentication-hmac>
2. <https://datatracker.ietf.org/doc/html/rfc7519>

## 管理接口

管理接口的调用都需要通过[Marketing API（OAuth 2.0）](https://open.oceanengine.com/doc/index.html?key=ad&type=api&id=1696710502277120)授权。

### 新增公钥

客户将公钥保存到平台。

为支持切换密钥的场景，客户最多可使用两对公/私钥对，两对的label约定为“primary”、“backup”。

为确保客户使用了正确的公钥格式和签名算法，在新增公钥时有一步验签的动作，客户使用私钥对指定文本“BYTEDANCE”签名。

**请求地址**

<https://ad.oceanengine.com/open_api/2/event_manager/auth/add_public_key>

**请求方法**

POST

**请求参数**

| **字段** | **类型** | **是否必选** | 描述 |
| --- | --- | --- | --- |
| advertiser\_id | long | 是 | 客户ID |
| credential | string | 是 | 密钥对label，枚举值：“primary”或“backup” |
| pubkey | string | 是 | RSA公钥（1024位）文本内容，格式为[X.509 Certificate Subject Public Key Info](https://datatracker.ietf.org/doc/html/rfc7468#section-13)，可使用openssl生成，详见附录。注意：使用openssl生成的公钥是多行文本，需要使用换行符“\n”将多行文本转为单行。 |
| signature | string | 是 | 使用RS256签名算法和私钥对字符串“BYTEDANCE”签名后的结果，可使用openssl计算，详见附录 |

**请求示例**

```
{
    "advertiser_id": 123456789,
    "credential": "primary",
    "pubkey": "-----BEGIN PUBLIC KEY-----\nMIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQDSLWmtc3edSzIp7d2QoHCnjNNx\ncjGURr/y43fuCAjFZFhxYiIhrNF0p5gmI/c3B8o05aTf8BDXU3BFjZupyzTHoRKr\nHg4hoPvn2EH9UfxICp1bCtIcwe1a4rjCBd/CAaF6DCKp19Pvci1+e8xTJXU0ERVM\n6K5yu6aixC2y0NUMdwIDAQAB\n-----END PUBLIC KEY-----",
    "signature": "I9bQJLPV2Q3YoNIlkjCVm532VMyACguPFKCCWVy5UW3W7pKv/sshnzRZNM7kBoibcqGDG9DdTTxf3gIbAH6SF5JgDZt48ihHJMXz8zxbKy7ZNbpVSwAuG8FjOxWfsQhx1+zt1ViGmMTJHj3UQKLnMNO7ysOrVR3ZrYPIong1QZc="
}
```

**应答字段**

| **字段** | **类型** | **是否必选** | 描述 |
| --- | --- | --- | --- |
| code | int | 是 | 返回码 |
| message | string | 是 | 返回信息 |
| data.pubkey | object | 是 | 公钥 |

**应答示例**

```
{
    "code": 0,
    "message": "OK",
    "request_id": "20211108002318010225146063075F0CDB",
    "data": {
        "pubkey": {
            "advertiser_id": 123456789,
            "credential": "primary",
            "key_id": 1715787214399502,
            "pubkey": "-----BEGIN PUBLIC KEY-----\nMIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQDSLWmtc3edSzIp7d2QoHCnjNNx\ncjGURr/y43fuCAjFZFhxYiIhrNF0p5gmI/c3B8o05aTf8BDXU3BFjZupyzTHoRKr\nHg4hoPvn2EH9UfxICp1bCtIcwe1a4rjCBd/CAaF6DCKp19Pvci1+e8xTJXU0ERVM\n6K5yu6aixC2y0NUMdwIDAQAB\n-----END PUBLIC KEY-----"
        }
    }
}
```

### 删除公钥

客户删除公钥。

鉴权关闭状态下，可直接删除；鉴权开启状态下，平台会检查最近一段时间（24小时）是否有待删除密钥的使用记录，如果没有则可删除，否则删除失败。

**请求地址**

<https://ad.oceanengine.com/open_api/2/event_manager/auth/del_public_key>

**请求方法**

POST

**请求参数**

| **字段** | **类型** | **是否必选** | **说明** |
| --- | --- | --- | --- |
| advertiser\_id | long | 是 | 客户ID |
| credential | string | 是 | 密钥对label，枚举值：“primary”或“backup” |

**请求示例**

```
{
    "advertiser_id": 123456789,
    "credential": "primary"
}
```

**应答字段**

| **字段** | **类型** | **是否必选** | 描述 |
| --- | --- | --- | --- |
| code | int | 是 | 返回码 |
| message | string | 是 | 返回信息 |

**应答示例**

```
{
    "code": 0,
    "message": "OK",
    "request_id": "20211108002315010225146063075F0C7F",
    "data": {}
}
```

### 查询公钥

客户查询公钥。

**请求地址**

<https://ad.oceanengine.com/open_api/2/event_manager/auth/get_public_key>

**请求方法**

GET

**请求参数**

| **字段** | **类型** | **是否必选** | **说明** |
| --- | --- | --- | --- |
| advertiser\_id | long | 是 | 客户ID |
| credential | string | 是 | 密钥对label，枚举值：“primary”或“backup” |

**请求示例**

```
https://ad.oceanengine.com/open_api/2/event_manager/auth/get_public_key?advertiser_id=123456789&credential=primary
```

**应答字段**

| **字段** | **类型** | **是否必选** | 描述 |
| --- | --- | --- | --- |
| code | int | 是 | 返回码 |
| message | string | 是 | 返回信息 |
| data.pubkey | object | 是 | 公钥 |

**应答示例**

```
{
    "code": 0,
    "message": "OK",
    "request_id": "20211108003335010225084217045DED26",
    "data": {
        "pubkey": {
            "advertiser_id": 123456789,
            "credential": "primary",
            "key_id": 1715787214399502,
            "pubkey": "-----BEGIN PUBLIC KEY-----\nMIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQDSLWmtc3edSzIp7d2QoHCnjNNx\ncjGURr/y43fuCAjFZFhxYiIhrNF0p5gmI/c3B8o05aTf8BDXU3BFjZupyzTHoRKr\nHg4hoPvn2EH9UfxICp1bCtIcwe1a4rjCBd/CAaF6DCKp19Pvci1+e8xTJXU0ERVM\n6K5yu6aixC2y0NUMdwIDAQAB\n-----END PUBLIC KEY-----"
        }
    }
}
```

### 查询全部公钥

客户查询公钥。

**请求地址**

<https://ad.oceanengine.com/open_api/2/event_manager/auth/get_all_public_keys>

**请求方法**

GET

**请求参数**

| **字段** | **类型** | **是否必选** | **说明** |
| --- | --- | --- | --- |
| advertiser\_id | long | 是 | 客户ID |

**请求示例**

```
https://ad.oceanengine.com/open_api/2/event_manager/auth/get_all_public_keys?advertiser_id=123456789
```

**应答字段**

| **字段** | **类型** | **是否必选** | 描述 |
| --- | --- | --- | --- |
| code | int | 是 | 返回码 |
| message | string | 是 | 返回信息 |
| data.pubkeys | array[object] | 是 | 公钥列表 |

**应答示例**

```
{
    "code": 0,
    "message": "OK",
    "request_id": "202111080049320102252431180450CA0F",
    "data": {
        "pubkeys": [
            {
                "advertiser_id": 123456789,
                "credential": "backup",
                "key_id": 1715788859752462,
                "pubkey": "-----BEGIN PUBLIC KEY-----\nMIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQDO5PPNkqBX9xhBKRKFDp/yS0eP\nHUKt005sJgvlGKFISAFiqx2EkvxzjdDWVBfrvzE+BQF1K8lR0ZKKf7LOLvlrjJu+\n3dXHhFG8MVlMNJYiEIkY6CQ/e3auMbhY712PMk+RUY84uX3spRfCcsSz8/3wLVbG\nd0LoDpI9CjcKpT7WBQIDAQAB\n-----END PUBLIC KEY-----"
            },
            {
                "advertiser_id": 123456789,
                "credential": "primary",
                "key_id": 1715787214399502,
                "pubkey": "-----BEGIN PUBLIC KEY-----\nMIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQDSLWmtc3edSzIp7d2QoHCnjNNx\ncjGURr/y43fuCAjFZFhxYiIhrNF0p5gmI/c3B8o05aTf8BDXU3BFjZupyzTHoRKr\nHg4hoPvn2EH9UfxICp1bCtIcwe1a4rjCBd/CAaF6DCKp19Pvci1+e8xTJXU0ERVM\n6K5yu6aixC2y0NUMdwIDAQAB\n-----END PUBLIC KEY-----"
            }
        ]
    }
}
```

### 开启鉴权

客户开启鉴权。

鉴权关闭状态下，鉴权失败的转化数据不会被丢弃，同时返回鉴权失败原因，方便客户调试定位。**开启鉴权后，鉴权失败的转化数据将被丢弃。****平台会缓存客户的鉴权开启状态，缓存时长约3分钟，这意味着，最长会有3分钟的过渡时间。**

客户确认上线后有成功回传转化，且没有鉴权失败（观察24h以上，最长不超过一周)之后，可请求“开启鉴权”接口，平台检查是否满足开启条件，如果满足则开启，否则开启失败。

**请求地址**

<https://ad.oceanengine.com/open_api/2/event_manager/auth/enable>

**请求方法**

POST

**请求参数**

| **字段** | **类型** | **是否必选** | **说明** |
| --- | --- | --- | --- |
| advertiser\_id | long | 是 | 客户ID |

**请求示例**

```
{
    "advertiser_id": 123456789
}
```

**应答字段**

| **字段** | **类型** | **是否必选** | 描述 |
| --- | --- | --- | --- |
| code | int | 是 | 返回码 |
| message | string | 是 | 返回信息 |

**应答示例**

```
{
    "code": 0,
    "message": "OK",
    "request_id": "2021110420080101022514515206119639",
    "data": {}
}
```

### 关闭鉴权

客户关闭鉴权。**关闭鉴权后，回传的转化数据即使鉴权失败也不会被丢弃。****平台会缓存客户的鉴权开启状态，缓存时长约3分钟，这意味着，最长会有3分钟的过渡时间。**

**请求地址**

<https://ad.oceanengine.com/open_api/2/event_manager/auth/disable>

**请求方法**

POST

**请求参数**

| **字段** | **类型** | **是否必选** | **说明** |
| --- | --- | --- | --- |
| advertiser\_id | long | 是 | 客户ID |

**请求示例**

```
{
    "advertiser_id": 123456789
}
```

**应答字段**

| **字段** | **类型** | **是否必选** | 描述 |
| --- | --- | --- | --- |
| code | int | 是 | 返回码 |
| message | string | 是 | 返回信息 |

**应答示例**

```
{
    "code": 0,
    "message": "OK",
    "request_id": "2021110420232701022514515205135196",
    "data": {}
}
```

### 查询鉴权开启状态

客户查询鉴权开启状态。

**请求地址**

<https://ad.oceanengine.com/open_api/2/event_manager/auth/get_auth_status>

**请求方法**

GET

**请求参数**

| **字段** | **类型** | **是否必选** | **说明** |
| --- | --- | --- | --- |
| advertiser\_id | long | 是 | 客户ID |

**请求示例**

```
https://ad.oceanengine.com/open_api/2/event_manager/auth/get_auth_status?advertiser_id=123456789
```

**应答字段**

| **字段** | **类型** | **是否必选** | 描述 |
| --- | --- | --- | --- |
| code | int | 是 | 返回码 |
| message | string | 是 | 返回信息 |
| data.enabled | bool | 是 | 鉴权是否开启 |

**应答示例**

```
{
    "code": 0,
    "message": "OK",
    "request_id": "2021110420080901022514515206119761",
    "data": {
        "enabled": true
    }
}
```

### 错误

| code | message | 描述 |
| --- | --- | --- |
| 0 | OK | 成功 |
| 40001 | 具体msg见返回信息 | 请求方错误 |
| 50000 | SYS\_ERROR | 系统错误 |

**错误示例**

```
// 开启鉴权失败，必须至少有一个公钥
enable auth failed, at least one key is required to enable auth
​
// 删除公钥失败，公钥不存在
delete public key failed, the key does not exist
​
// 新增公钥失败，signature字段请使用标准base64编码格式
signature invalid, please use base64 standard encoding
​
// 公钥内容非法，请使用标准base64编码的PKCS1或PKCS8公钥格式
public key invalid, please use base64 standard encoded PKCS1 or PKCS8 public key
​
// 签名验证失败，请使用rs256签名算法生成你的签名
signature verification failed, please use rs256 algorithm
​
// 添加公钥失败，相应“credential”的公钥已存在，如果你想更换公钥，可以先新增一个公钥，再删除另一个
public key already exists, if you want to rotate the key, please add a new public key first, and then delete the old one
​
// 删除公钥失败，鉴权开启状态下至少需要保留一个公钥
delete public key failed, when auth is enabled, at least one key is required
​
// 开启鉴权失败，至今没有回传转化，或者仍有不含签名或签名失败的转化回传，请检查转化回传是否签名正确，可以在几小时后进行重试（平台默认的观察周期是一天）
enable auth failed, there is no request or there are still requests with auth failure, please check and try again in a few hours
​
// 开启鉴权失败，平台的观察时长尚未满足（平台默认的观察周期是一天）
enable auth failed, observation time is insufficient, please wait a few hours and try again
​
// 公钥的credential非法，credential的值必须为“primary”或“backup”
credential invalid, credential must be primary or backup
​
// 公钥的长度不允许超过1024个字符，请使用文档中说明的方式生成公钥
public key too long, cannot exceed 1024 characters
​
// 删除公钥失败，公钥最近仍在使用（平台默认的观察周期是一天）
delete public key failed, the public key is still in use, please check and try again in a few hours
```

## 附录

### 通过命令行方式生成密钥对并签名

**1. 生成RSA密钥对**

生成1024位RSA私钥private-key.pem：

```
openssl genrsa -out private-key.pem 1024
```

备注：可通过如下命令将私钥private-key.pem转换为[pkcs8](https://en.wikipedia.org/wiki/PKCS_8)格式pkcs8.pem：

openssl pkcs8 -topk8 -inform PEM -in private-key.pem -outform pem -nocrypt -out pkcs8.pem

从私钥中提取公钥public-key.pem：

```
openssl rsa -in private-key.pem -pubout -outform PEM -out public-key.pem
```

查看公钥文本内容：

```
cat public-key.pem
-----BEGIN PUBLIC KEY-----
MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQDSLWmtc3edSzIp7d2QoHCnjNNx
cjGURr/y43fuCAjFZFhxYiIhrNF0p5gmI/c3B8o05aTf8BDXU3BFjZupyzTHoRKr
Hg4hoPvn2EH9UfxICp1bCtIcwe1a4rjCBd/CAaF6DCKp19Pvci1+e8xTJXU0ERVM
6K5yu6aixC2y0NUMdwIDAQAB
-----END PUBLIC KEY-----
```

**2. 计算signature**

使用私钥private-key.pem计算字符串"BYTEDANCE"的签名：

```
echo -n "BYTEDANCE" | openssl dgst -sha256 -binary -sign private-key.pem  | openssl enc -base64 | tr -d '\n'
```

上面命令的输出结果即生成的签名：

```
I9bQJLPV2Q3YoNIlkjCVm532VMyACguPFKCCWVy5UW3W7pKv/sshnzRZNM7kBoibcqGDG9DdTTxf3gIbAH6SF5JgDZt48ihHJMXz8zxbKy7ZNbpVSwAuG8FjOxWfsQhx1+zt1ViGmMTJHj3UQKLnMNO7ysOrVR3ZrYPIong1QZc=
```

