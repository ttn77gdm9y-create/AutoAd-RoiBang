# Changelog

## v0.4.20 - 2026-06-09

- 本地引力素材库默认按“上传到引力时间”倒序展示，新素材排在前面；后端接口也加测试锁定这个默认排序。
- 引力素材库接口新增专辑、文件夹、引力创建日期范围筛选，并返回可选专辑/文件夹，供前端下拉使用。
- 本地素材库筛选区改为业务化布局：专辑、文件夹、表现窗口、引力上传起止时间、素材搜索和一键重置。
- 素材表格支持 7 天 / 30 天表现窗口切换，只展示当前窗口的消耗、转化、ROI，减少宽表格拥挤。
- 素材表格字段顺序优化为专辑、文件夹、素材名、表现数据、引力素材 ID、MD5、上传到引力时间、最近同步时间、推送覆盖情况。
- 切换产品或筛选条件时会清空已选素材和旧推送预览，避免把不可见的旧素材带入后续真实推送预览。
- 安全边界不变：本版本只改本地素材库查询和页面展示，不下载素材文件、不上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.19 - 2026-06-09

- 引力素材同步改为按绑定专辑调用真实素材报表接口，支持按 `album_id`、日期范围和分页读取 7 天 / 30 天表现数据。
- 修正素材匹配口径：使用引力返回的 `gravity_material_id` 匹配本地素材，即使 `material_id` 为空也能写入表现汇总。
- 本地表现汇总新增首日 ROI 写入，和消耗、展示、点击、转化一起区分 7 天 / 30 天窗口保存。
- 引力素材库接口和页面新增 `7天ROI`、`30天ROI` 字段，并支持按 ROI 排序；没有报表数据时继续显示 `-`，不伪装成 0。
- 同步结果摘要补充报表读取页数、报表行数和写入表现汇总行数，方便判断数据是否真正入库。
- 安全边界不变：本版本只读取引力报表并写入 RoiBang 本地数据库，不下载素材文件、不上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.18 - 2026-06-09

- 全站大表接入统一表格布局：左侧菜单固定、页面内容区不再被宽表格撑出横向滚动。
- 产品账户库、引力素材库、任务中心、自动化工作台、投放建议工作台和结果摘要表格启用固定表头；向下滚动时仍能看到列名。
- 表格横向滚动限制在表格内部，分页区域固定在表格底部，避免翻页控件被挤到左侧菜单外。
- 产品账户库单列表格区域改为横跨整行，长字段和 20/50/100 分页不会破坏页面布局。
- 新增前端单元测试锁定统一表格布局、固定表头、内部横向滚动和分页基础体验。
- 安全边界不变：本版本只改前端展示体验，不触发真实同步、补材、预推送、创建广告或修改投放动作。

## v0.4.17 - 2026-06-09

- 产品自动化配置页的定时任务从“勾选启用”改为明确的“开启/关闭”开关表，逐项展示任务类型、当前状态和开关。
- 产品配置允许把某个产品的所有定时任务全部关闭；关闭后只影响后续定时触发，不删除历史数据，也不中断已经开始的任务。
- 保存配置结果新增“已开启定时任务 / 已关闭定时任务”中文摘要，避免看不出哪些任务已被关闭。
- 任务请求预演遇到已关闭任务时，会明确提示该产品已关闭该任务，不再只显示空结果。
- 从产品自动化配置页移除“AI 模板草稿”模块，避免创建模板能力混入产品配置页面。
- 安全边界不变：本版本不执行真实同步、补材、预推送、创建广告或修改投放动作。

## v0.4.16 - 2026-06-08

- 引力素材“提前铺货到巨量账户”改为只使用产品账户库中负责人为郭靖的启用账户；停用账户、非郭靖负责人账户不会进入铺货预览。
- 引力素材库第 4 步会自动使用本地可铺货素材和郭靖启用账户生成提前铺货预览，不再要求用户手动逐个选择素材和账户。
- 提前铺货预览新增账户来源、目标负责人、批次数和铺货数量摘要；默认按 50 条一批组织，参考源素材预推送的批次化思路。
- 超大铺货预览超过单次确认上限时自动阻断真实执行，只展示前 500 条样例和中文说明，避免一次性确认几十万条媒体动作。
- 产品账户库支持按产品名或产品 Key 搜索，并可按账户来源筛选；历史补全、人工导入和示例账户会分开展示。
- 创建计划页面明确：引力素材只使用已经提前铺货并回填媒体素材 ID 的素材，创建计划不会临时上传素材。
- 安全边界不变：本版本只生成预览和本地结果，不自动上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.15 - 2026-06-08

- 本地素材库主表改为只展示可铺货素材：引力禁用、本地停用、拒审、缺 MD5 的素材不会出现在主表里，避免误选。
- 本地素材库表格改为更直观的业务字段顺序：专辑、素材名、7 天/30 天消耗与转化、引力素材 ID、MD5、引力创建时间、最近同步时间、推送覆盖情况、文件夹。
- 素材列表改为后端真实分页和排序，默认每页 100 条，可切换 20/50/100；消耗、转化、创建时间、同步时间等字段可排序。
- 移除“素材范围”“可用于后续”“有表现数据”等容易误解的前端文案；没有真实表现汇总时显示 `-`，不再用 0 误导。
- 产品账户库批量修改新增“修改范围”：当前筛选结果、当前勾选账户、粘贴账户 ID；粘贴 ID 只会修改当前筛选结果里命中的账户，不会越过产品/状态筛选。
- 主要可分页表统一默认每页 100 条，页数选项统一为 20/50/100。
- 安全边界不变：本版本不下载素材文件、不上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.14 - 2026-06-08

- 修复“更新引力素材完成但本地素材库仍为空”的根因：引力素材列表接口下钻文件夹时，需要把文件夹 ID 当作下一层 `album_id` 请求，而不是继续传 `folder_id`。
- 引力素材同步测试改为模拟真实接口行为，锁定“顶层专辑返回文件夹、文件夹 ID 再返回素材”的链路，避免测试通过但真实流程不通。
- 更新引力素材完成但发现 0 个素材时，会明确提示“已扫描多少文件夹但没有发现素材”，引导检查绑定范围或接口返回，不再伪装成普通成功。
- 本地素材库空数据提示里的按钮改为“立即更新引力素材”，会在本页自动完成更新预览检查并启动固定更新任务；另保留“查看第 2 步详情”用于看明细。
- 页面文案把“同步预览 / 资料同步”统一改成“更新预览 / 更新素材”，减少技术感。
- 安全边界不变：本版本不下载素材文件、不上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.13 - 2026-06-08

- 修复绑定上层引力专辑后本地素材库仍为空的问题：更新引力素材会自动扫描专辑下的文件夹和子文件夹，直到拿到真正素材再写入本地素材库。
- 更新引力素材结果统计改为区分“扫描文件夹、发现素材、入库素材”，不再把文件夹数量误报成素材数量。
- 本地素材库默认展示“全部素材”，让用户先看到同步到本地的素材，再通过资格状态判断哪些能用于后续推送。
- 引力素材库页面改为 4 个业务步骤：1 绑定素材来源、2 更新引力素材、3 本地素材库、4 推送引力素材到巨量账户。
- 安全边界不变：本版本不下载素材文件、不上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.12 - 2026-06-08

- 引力素材库页面新增完整流程提示：绑定素材来源、更新引力素材、查看本地素材、检查可用素材、生成推送预览。
- 页面顶部会用中文说明当前卡在哪一步和下一步该做什么，例如先绑定来源、检查引力 Token、更新引力素材、选择可用素材或生成推送预览。
- 更新引力素材准备检查新增“下一步”结构化结果，页面和结果中心都能显示更清楚的业务建议。
- 引力素材库页面内的“去同步资料”改为“去更新素材”，并在本页定位到更新区域，减少必须跳去自动化工作台的困惑。
- 安全边界不变：本版本不上传素材、不下载素材文件、不创建广告、不修改预算、出价或项目状态。

## v0.4.11 - 2026-06-06

- 账户备注页面的“账户 ID”输入框保留手填/大段粘贴能力，并明确提示只能粘贴数字账户 ID。
- 账户备注预览支持从 CSV / Excel 粘贴文本中读取“账户 ID”列；如果误粘账户名，会直接阻断并用中文指出问题行。
- 新生成的账户备注 JSON 默认开启固定 HTTP 执行配置，并改为读取 `data/secrets/oceanengine-workbench-session.local.json`；仍必须经过页面预览和“确认执行”才会真实修改。
- 账户备注执行脚本增加数字账户 ID 兜底校验，防止绕过页面直接把账户名写入真实执行配置。
- 产品账户库表格翻页固定在可视区域左下，表格横向滚动时不再把翻页挤到侧边栏外。
- 安全边界不变：本版本不自动执行真实账户备注修改、不上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.10 - 2026-06-06

- 产品账户库下载模板改为和“账户明细”一致的中文表头：产品、产品 Key、账户 ID、账户名、渠道、负责人、状态、备注、说明。
- 账户导入继续兼容旧英文表头，同时支持中文表头 CSV / Excel / 粘贴文本，避免历史文件失效。
- 账户库导出也改为中文表头，和页面展示保持一致。
- 真实修改账户备注在预览阶段检查固定执行配置、HTTP 开关、工作台 Cookie 和 CSRF Token；配置不完整时直接显示“真实修改未执行”，不再启动一个注定失败的后台任务。
- 账户备注执行脚本和任务详情中的英文拦截原因改成中文说明，并明确“本次未修改任何账户备注”。
- 用户可见名称统一：账户备注执行改为“真实修改账户备注”，“同步引力素材资料到本地”改为“更新引力素材”，“引力素材资格汇总”改为“检查可用素材”。
- 安全边界不变：本版本不临场执行真实备注修改、不上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.9 - 2026-06-06

- 引力素材库页面新增“同步引力素材资料到本地”卡片：可在本页做同步准备检查、生成同步预览、启动固定同步任务，并在本页查看任务进度。
- 明确同步边界：这一步只读取素材名、归属专辑/文件夹、引力素材 ID、MD5、状态和表现数据；不会下载素材文件、不会上传到巨量账户、不会影响每天从真实巨量账户同步来的素材数据。
- 新增同步准备检查 API：检查产品绑定、目标专辑范围、Token 文件和字段完整性；只读本地数据库和本地 Token 文件，不访问引力接口、不展示 Token 明文。
- 同步任务完成后，引力素材库页面会自动刷新准备检查和本地素材列表，减少用户去任务中心/结果中心找日志。
- 工作流、结果目录和页面文案从“引力素材同步入库”统一改为“同步引力素材资料到本地”，减少技术词和误解。
- 补充后端 API 测试和前端单元测试，锁定同步准备检查、固定 workflow 请求参数、同步边界文案。
- 安全边界不变：本版本不下载素材文件、不上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.8 - 2026-06-05

- 重设计引力素材库页面结构：按“绑定来源 → 同步入库 → 查看素材 → 生成推送预览”的顺序展示，减少用户误解。
- 产品-专辑绑定改为“绑定素材来源”，引力专辑/文件夹下拉按目标专辑分组展示，并明确区分“绑定整个专辑”和“只绑定文件夹”。
- “素材资格明细”改为“本地素材库”，筛选项增加“素材范围”标签，上传状态在页面展示为“推送状态”。
- “上传到目标账户”改为“推送引力素材到巨量账户”，明确这是后续真实媒体动作：必须先生成预览并核对账户名、账户 ID、素材 ID、MD5，真实推送前仍需输入“确认执行”。
- 新增轻量前端测试，锁定引力目标专辑和文件夹必须分组展示。
- 安全边界不变：本版本不上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.7 - 2026-06-05

- 修正引力目标专辑匹配逻辑：目标专辑下面有多个子文件夹时，仍优先把顶层目标专辑识别为“已匹配”，不再误判为“多个匹配”。
- 产品-专辑绑定页的 3 个目标专辑可直接填入顶层专辑；子文件夹继续作为候选保留，供后续需要细分时选择。
- 补充回归测试，锁定“顶层目标专辑 + 多个子文件夹”场景。
- 安全边界不变：本版本不上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.6 - 2026-06-05

- 更新 README 和 agent 规则，明确 RoiBang-v2 当前方向是“固定脚本 + 工作流注册表 + 自动化工作台 + 中文结果汇报”，不再只是一组分散脚本。
- 补充 AutoX 借鉴边界：借鉴脚本中心、任务调度、日志、artifact 读取和 AI 触发只读工作流；不照搬自由调用广告平台的能力。
- 补充工作流开发规则：新增业务能力优先登记为白名单 workflow，写清中文名、风险等级、参数、结果 artifact、是否允许 AI 自动运行、是否真实投放动作。
- 强化本地服务重启规则：改完前后端后必须重启用户实际使用的端口，并用同一端口验收，避免前端连接旧后端。
- 本版本只更新文档和版本记录，不修改业务代码，不执行真实投放动作。

## v0.4.5 - 2026-06-05

- 引力素材库只允许绑定和同步 3 个目标专辑：黑旗-奇门（塔防）、魔兽开箱子、黑旗-6480 咸鱼-微小合集。
- 引力专辑树页面不再把全部原始专辑作为主选择项展示，只展示目标专辑匹配状态；原始专辑/文件夹仅保留在折叠技术明细中。
- 引力素材同步入库会在访问引力前检查产品绑定，发现目标范围外的绑定会直接阻断，避免误同步其他游戏素材。
- 产品账户库新增账户明细表和按勾选账户批量修改能力，支持把账户状态改为启用、暂停或停用，并继续保留预览确认。
- 项目管理配置、项目管理执行预览、素材预推送和自动化预推送都会过滤或阻断停用/暂停账户，避免停用账户继续被管理或接收素材。
- 引力素材上传目标账户只展示启用账户；页面、预览、结果继续同时展示账户 ID 与账户名。
- 安全边界不变：本版本不临场执行真实投放动作；上传素材仍必须经过页面预览、中文摘要、明细表和“确认执行”。

## v0.4.3 - 2026-06-05

- 修正引力鉴权请求头：`authorization` 按 `data/gravity_token.json` 中保存的值原样传递，不再自动补 `Bearer`，避免引力接口返回“登录异常”。
- 覆盖引力只读探测和引力素材库共用 client，确保素材同步、资格汇总、上传预览/状态查询复用同一鉴权规则。
- 兼容引力真实专辑树结构 `data.tree` 和节点名称字段 `label`，素材库页面可从最近只读探测结果读取专辑/文件夹绑定选项。
- 兼容引力素材列表真实结构 `data.list[].material`，正确读取素材 ID、文件名、MD5、状态、媒体拒审次数，并避免把引力素材 ID 误当作巨量媒体素材 `video_id` 入库。
- 优化“引力 Token 获取/刷新”缺环境变量时的中文展示：显示“已阻塞”“缺少引力登录环境变量”，并明确说明未访问引力、未生成 Token、未执行业务动作。
- 自动化工作台风险标签支持低/中/高完整展示，避免高风险被误显示成低风险。
- 安全边界不变：本版本不上传素材、不创建广告、不修改预算、出价或项目状态；`data/gravity_token.json` 仍不提交。

## v0.4.2 - 2026-06-04

- 新增“引力 Token 获取/刷新”工作流：通过固定 headless 浏览器脚本从引力登录会话中采集鉴权请求头，并写入 `data/gravity_token.json`。
- 新增固定脚本 `scripts/run_gravity_token_refresh.py`，只从环境变量名称读取账号密码，不在前端页面传账号、密码或 Token 明文。
- 自动化工作台注册引力 Token 获取/刷新任务，风险标记为中，AI 自动运行标记为不允许。
- Token 获取结果只展示中文摘要、文件状态、字段状态、有效期和剩余时间；artifact 与 CLI 输出均不包含 Token、密码或 Authorization 明文。
- 结果中心新增“引力 Token 获取/刷新”结果类型，便于回看本地鉴权刷新结果。
- 安全边界不变：本版本不上传素材、不创建广告、不修改预算、出价或项目状态。

## v0.4.1 - 2026-06-04

- 创建计划新增“素材来源”选择：默认继续使用源素材账户，也可选择引力素材库。
- 引力素材库模式只使用 `gravity_upload_tasks.status = completed` 且已回填合法媒体素材 ID 的素材，不会上传素材、不会创建广告。
- 创建计划按目标账户匹配引力上传结果：同一个引力素材上传到不同账户时，分别使用各账户自己的 `video_id`。
- 创建计划预览、生成命令、固定脚本、计划详情和执行前复核摘要都会展示或传递素材来源。
- 创建计划页面新增素材来源分段选择和中文安全提示，避免把引力素材误认为可直接创建。
- 安全边界不变：真实创建仍必须经过计划明细、执行前复核、中文摘要和“确认执行”。

## v0.4.0 - 2026-06-04

- 新增引力素材上传预览确认链路：先按产品、目标账户和引力素材生成中文预览，核对账户名、账户 ID、素材 ID、MD5、是否账户已有、是否需上传。
- 新增 `gravity_upload_tasks` 本地上传账本，记录产品、引力素材 ID、目标账户 ID、目标账户名、引力 task_id、上传状态、媒体素材 ID 和失败原因。
- 新增固定上传脚本 `scripts/run_gravity_upload_to_account.py`，真实 `upload_material` 只能在页面输入“确认执行”后由固定脚本读取预览 JSON 执行。
- 新增只读状态刷新脚本 `scripts/run_gravity_upload_status_poll.py`，只查询引力上传 task 状态，并在本地账户素材库同步到合法 `video_id` 后回填账本。
- 引力素材库页面新增“上传到目标账户”和“上传状态复盘”：支持勾选合格素材、下拉选择目标账户、生成预览、确认执行、查看上传任务和刷新本地账本。
- 创建计划仍未接入引力素材；只有后续 `gravity_upload_tasks.status = completed` 且有合法媒体素材 ID 的素材，才允许进入下一版本创建计划接入。
- 安全边界不变：AI 不临场执行真实上传；真实上传必须通过页面预览、中文摘要、明细表和“确认执行”。

## v0.3.3 - 2026-06-04

- 新增引力素材资格汇总 workflow 和固定脚本，基于本地已同步素材计算“可用于后续 / 不可用 / 缺 MD5 / 已上传 / 未上传 / 有表现数据”。
- 引力素材库 API 新增资格字段：资格状态、不可用原因、上传状态、媒体素材 ID，并展示最近资格汇总结果。
- 引力素材库页面升级为“素材资格明细”，顶部展示素材资格体检数字，支持按资格、缺 MD5、上传状态、表现数据筛选。
- 资格判断只使用本地数据和已同步表现汇总；不会上传素材、不会创建广告、不会修改预算、出价或项目状态。
- 新增 `configs/gravity-material-qualification.example.json` 示例配置，不包含真实 Token 或密钥。

## v0.3.2 - 2026-06-04

- 新增“引力素材库”页面，支持产品-专辑/文件夹绑定、素材列表查看、最近同步结果展示。
- 新增引力素材本地同步入库 workflow 和固定脚本，只读读取引力素材库并写入本地素材池，不上传素材、不创建广告。
- 新增 `product_gravity_album_bindings` 本地绑定表，引力素材以 `source = gravity_engine` 写入本地源素材数据。
- 自动化工作台新增“引力素材同步入库”，参数支持产品、Token 文件、每页素材数、最多页数选择，并保留运行预览。
- 新增引力素材库 API：绑定保存/删除、绑定列表、素材列表、从最近探测结果读取专辑树。
- 新增引力素材同步示例配置 `configs/gravity-material-sync.example.json`，不包含真实 Token。
- 安全边界不变：本版本不上传素材、不接创建计划、不调用真实投放动作。

## v0.3.1 - 2026-06-04

- 引力素材库只读探测兼容文档里的 `authorization` Token 文件格式，同时保留旧的 `jwt_token` 兼容。
- 自动处理 `Bearer` 前缀：Token 文件里写不写 `Bearer` 都能生成正确的只读接口请求头。
- 新增本地 Token 状态判断：缺文件、缺字段、已过期会在访问外部接口前阻止，并给出中文原因。
- 探测结果新增鉴权字段状态、Token 状态、过期时间和剩余时间；结果 JSON 与页面仍不展示 Token 明文。
- 工作流中心最近结果会直接展示引力 Token 状态和阻塞原因，避免只看到“外部只读调用 0”。
- 上传素材接口仍不在可执行范围内；上传任务状态只允许只读查询，不会为了验证而上传素材。

## v0.3.0 - 2026-06-03

- 引力素材库只读探测从“本地 token 检查”升级为“文档字段核验 + 外部只读接口探测”。
- 新增探测范围下拉：可选择“本地鉴权与文档字段核验”或“外部只读接口探测”。
- 新增样本数量下拉：只读取少量素材样本做字段核验，不做素材同步入库。
- 工作流预览里的下拉参数改为中文展示，并阻断菜单外的非法参数值。
- 探测结果新增中文字段核验表、接口核验表、样本素材表，用人话说明素材 ID、MD5、专辑/文件夹、状态、拒审字段、报表、上传任务状态的核验结论。
- 外部探测只调用只读查询接口；不会调用 `upload_material` 上传素材，不创建广告，不修改投放。
- 继续保护引力 JWT Token，结果 JSON 和页面摘要不展示 token 明文。

## v0.2.1 - 2026-06-03

- 自动化工作台升级为“定时任务状态看板 + 手动补跑入口 + 本页结果验收”。
- 任务菜单新增最近状态、最近运行时间、最近结果和结果文件，能直接看到定时任务或手动补跑的最近结果。
- 工作流参数新增业务控件类型，产品改为中文下拉选择，日期改为“今天 / 昨天 / 指定日期”选择。
- “刷新任务菜单”改为“刷新任务状态”，刷新后同步更新左侧任务状态和右侧最近结果。
- 本地开发启动脚本明确让前端连接 `8007` 后端，并检查新版工作流菜单接口，避免误连旧 `8000` 导致 `not found`。
- 前端默认 API 地址优先使用 `8007`，减少旧后端残留造成的页面错误。

## v0.2.0 - 2026-06-03

- 新增“自动化工作台”，把安全的固定脚本整理成可预览、可启动、可追踪的任务菜单。
- 新增后端工作流菜单、预览和运行接口，只允许登记过的只读同步、本地重算和探测任务。
- 注册首批安全任务：素材明细同步、每日报表同步、操作日志同步、源素材表现汇总、同步数据并重算建议、引力素材库只读探测。
- 新增引力素材库本地鉴权探测脚本，只读取本地 Token 文件，不上传素材、不创建广告、不修改投放。
- 结果中心新增自动化工作台相关结果类型，方便从当前任务跳转回看历史结果。
- 补充测试，锁定未知任务阻断、未登记参数阻断、固定命令运行、无真实投放动作和 token 不泄露。
