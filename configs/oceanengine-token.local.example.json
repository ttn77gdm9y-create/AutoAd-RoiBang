{
  "oceanengine_token": {
    "store_file": "data/secrets/oceanengine-tokens.local.json",
    "user_id": "default",
    "credentials_file": "data/secrets/oceanengine-app.local.json",
    "auth_code_env": "OCEANENGINE_AUTH_CODE",
    "refresh_lead_seconds": 900,
    "notes": [
      "This file is a template only. Do not write app secrets, auth codes, access tokens, or refresh tokens here.",
      "The token store path is ignored by git via data/secrets/."
    ]
  }
}
