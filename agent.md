# Agent Rules

## 协作流程

- 任何代码、功能、脚本、前后端行为改动之前，必须先给出明确方案，并和用户讨论确认后再修改文件。
- 方案必须说明：要改哪些文件、为什么改、业务边界、风险点、验证方式。
- 未经用户确认，不要直接开始改代码；除非用户明确要求的是只读检查、阅读文档、运行状态查询，或明确要求修改纯文档内容。
- 不要反复询问是否继续。方案确认后，除非确实需要系统权限或遇到阻断，直接按确认后的方案推进。
- 涉及英文专业词汇、接口字段、数据库表名、配置键、workflow/job 名称时，首次出现必须用中文解释含义；必要时使用 `英文：中文解释` 的格式，不要只堆英文缩写、表名或配置名。

## AI 角色边界

- AI 只负责改脚本、输出 JSON、写 JSON。
- AI 不负责临场执行业务动作；涉及业务动作时，只能产出固定脚本可读取的 JSON 配置或 JSON 结果。
- 如果要影响真实投放，必须由固定脚本读取 JSON，并在人工确认后执行。

## 系统边界

- 目标是 FastAPI + React 全量替换 Streamlit；Streamlit 验收完成前保留为旧版回退入口，不作为主入口继续增强。
- React/FastAPI 只做交互层、本地编排、中文摘要、任务状态和 artifact 展示。
- React/FastAPI 不直接拼平台业务接口，不直接替代现有 OpenAPI 调用层。
- 固定业务规则必须写在对应固定脚本或计划生成脚本里，不在前端或执行端偷偷改计划。
- 真实业务动作必须通过现有固定脚本执行。
- 真实执行必须走：中文摘要核对 -> 明细表 -> 输入 `确认执行` -> 固定脚本任务 -> 结果复盘。
- 后端启动脚本必须来自 allowlist，不接受前端传任意 shell。

## 测试规则

- 不默认写大范围测试，也不默认跑全量测试。
- 涉及真实执行业务链路、固定脚本 allowlist、确认执行、防旧计划误执行、账户 ID 与账户名展示、JSON 中文摘要等核心契约时，写少量后端窄测试。
- 纯前端文案、布局重排、按钮命名、信息分组、折叠展示、样式微调，可以不单独写测试。
- 默认只跑改动相关的最小测试文件；全量测试只在收口前、用户明确要求、或改到底层公共逻辑时运行。
- 每次方案里必须写清楚本次测试范围：哪些地方写测试、哪些不写、运行哪些命令、预计耗时。

## 禁止事项

- 不让 AI 临场执行业务动作。
- 不让 AI 猜账户、模板、预算、ROI、出价、素材数、项目数、单元数。
- 不提交本地密钥、token、真实账户和运行产物。
- 不新增复杂审批包、复核包、执行包作为主路径。
- 不绕过固定脚本和 JSON 配置。

## 页面和产物展示约束

- 所有 JSON、任务、日志、结果都必须有中文摘要；原始 JSON 只能作为折叠技术细节。
- 只要出现账户 ID，就必须同时展示账户名。
- 用户必须能在发起业务的页面上看到进度和结果，不要强迫去任务中心、结果中心、操作日志里翻。
- 任务中心、操作日志、结果中心主视图必须展示业务内容，不展示“标准输出长度”这类技术字段。
- 创建计划页必须明确区分“生成新计划”和“执行当前计划”。
- 旧计划和新计划必须一眼能区分，避免拿旧 artifact 执行。
- 页面操作逻辑要清晰、按钮少、中文可读，避免“生成计划 / 执行已有计划 / 读取摘要”这类让人困惑的主流程。
